+++
title = "brazen pin =0.0.3 is stale: 0.0.4 is published and 0.0.3 cannot parse current brazen config"
created = 1784955704
updated = 1784955704
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Foxglove finding 10 — third pin-drift incident (after bl-143e). Machine bz is 0.0.4; guard correctly refuses it; but installing the pinned 0.0.3 produces a bz that CANNOT parse the machine's ~/.config/brazen/config.toml ('data did not match any variant of untagged enum ProviderField') — so make install today installs a broken-on-this-machine adapter. Fix: bump Cargo.toml to brazen = "=0.0.4", fix any type/API fallout, update the README's three =0.0.3 mentions (Makefile derives automatically). Run the FULL gate — the httpmock e2e tests exercise real bz, so a 0.0.4 wire change will surface. Cargo.toml is contended (bl-8801 metadata work) — merge main eagerly. Consider whether a recurring-drift guard is worth filing separately (a check that the pinned version is the latest published is NOT wanted — pin-exact is the posture; the drift is a human cadence problem, note it in the ball on close).