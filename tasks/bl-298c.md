+++
title = "bash tool spec is unclear to models — vision-kiwi read it as a server-side bash prompt; align with codex's shell tool, which never confuses"
created = 1785645997
updated = 1785646033
claimant = "toolspec-fixer"
priority = 3
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Operator report 2026-08-02 (from yog): a conversation with agent vision-kiwi showed the bash tool definition is way too unclear — the model believed it was talking to a server-side interactive bash prompt. Operator: 'Have an agent take a look at what codex is doing. I've never had an issue there.'

Job: (1) read what the model actually receives — the tools array in a real request (yog workspaces keep every wire request: ~/.local/share/yog/workspaces/ops/steps/<conv>/NNN/request.json; also find the vision-kiwi conversation's transcript in ~/.local/share/yog/workspaces/*/agents/* for the literal misunderstanding, read-only). (2) Study codex's shell tool: the openai/codex repo's tool spec (name, description wording, schema — command array vs string, workdir, timeout, output contract) and note that gpt-5.x models are tuned on that shape — matching it matters as much as prose clarity. (3) Rewrite lernie's bash tool definition to be unambiguous: local non-interactive execution, where it runs (agent worktree cwd), what comes back (stdout+stderr+exit), statefulness between calls, no TTY. Keep lernie's tool grammar; this is the definition/description, not new machinery. Test: whatever asserts tool defs today must pin the new wording.