+++
title = "drop_oldest_steps overflow policy is inert by construction — subtract or justify"
created = 1784957131
updated = 1784957131
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Aphid follow-up from bl-a1a1. drop_oldest_steps names material (step records) that lives OUTSIDE every worktree, so at assembly it matches a no-op arm — inert by the identical argument that retired summarize. It survives as pre-v0.3.1 backward compatibility, arguably vacuous pre-0.0.1 (no released config can exist yet). Decide per the bl-a1a1 precedent (see its ARCH §5.2 rewrite and the parse-decline idiom rejects_retired_summarize_overflow): subtract it from OverflowPolicy with a parse-decline, regenerate schemas, update ARCH §5.2 — or document the concrete scenario that keeps it. Pre-0.0.1 is exactly the moment vocabulary can shrink for free.