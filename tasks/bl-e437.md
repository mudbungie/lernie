+++
title = "the seat's DHT walk still carries the three live defects yog measured: BEP 44 get/put ask the bootstrap routers (which never answer get), walks end dark (silent/refusing nodes hold frontier slots, the router is never re-asked, the bootstrap is counted as a result), and lockstep rounds wait out every silent node"
created = 1790394881
updated = 1790394882
claimant = "Urinalyses-Z1"
priority = 2
root_commit = "3efc0d263898c425a0ff2bb042938233e838f436"
+++
Port yog's proven walk (yog bl-f6e1, bl-9408, bl-d00f, bl-d9c1; evidence in yog REMOTE §13.7 ruling 3) into src/dht: bootstrap is a door asked find_node and never a result; frontier of the K closest replied/unasked/in-flight nodes (silent, refusing and unsendable nodes leave it); door re-asked when dry; sliding window of alpha queries each with its own deadline; put sends to every token holder at once. Defaults alpha 8, K 8, deadline 1 s, max_queries 64 (Config.round renamed deadline). Bootstrap list grows to yog's four mainline hosts. Tests: frontier, window and bootstrap suites with Router/Rotor/Mute fake moods. The observed-address vote is not ported: the seat never reads BEP 42 ip. yog bl-f519 (a put sometimes draws zero acks) is out of scope.