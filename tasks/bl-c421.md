+++
title = "gate: tests"
created = 1785129517
updated = 1785129606
claimant = "Thimble"
parent = "bl-9c06"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"

[[blockers]]
id = "bl-9c06"
on = "claim"
+++
PASS. Gate for bl-9c06 (delivered on main as 0d0587a).

- `make check` green at the delivered end state, twice independently: once as this ball's own gate run (100.00% coverage, 4857/4857 lines) and again inside `bl close`'s delivery gate after main was folded in (100.00% coverage, 4918/4918 lines, +0.00% change). fmt-check, clippy `-D warnings`, tarpaulin, and the `tests/install.rs` install contract all pass.
- The ball's own subject — determinism under parallel `cargo test --lib` — was re-verified by Thimble at the post-merge end state: 10 consecutive default-parallelism runs, all exit 0 (the whole 924-test lib suite, not just the three targets). `config_authors_a_commit`, `config_reports_a_declined_pass_as_a_clean_line`, and `replay_cli_lands_under_lernie_home` were green in all 10. Prior campaigns: Tiller 44/44, Chock 25/25.
- No assertion was weakened to achieve this: `Outcome::Quiet`, the `config/default unchanged: ` prefix assertion, and the `.config-author` absence check are unchanged; the diff only adds `with_lernie_home` wrapping.
- Coverage unaffected (test-side change only), so no new uncovered lines.