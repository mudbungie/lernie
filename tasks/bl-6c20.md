+++
title = "release 0.0.3: compaction-recursion fix (bl-a9eb) and pending toolset/descriptor fixes need a published version for yog's pin"
created = 1785288298
updated = 1785460148
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"

[[blockers]]
id = "bl-e4ef"
on = "close"
+++
DONE. lernie 0.0.3 published to crates.io 2026-07-30T07:37:53Z, declaring `brazen =0.0.5`.

Release PR #3 merged after the brazen pin bump (bl-e4ef, main 28bbdaf) landed and release-plz rebuilt the branch onto it — so 0.0.3 carries both the accumulated main work AND the =0.0.5 pin, satisfying yog bl-56bd's parity requirement verbatim: 'lernie 0.0.3 must declare `brazen =0.0.5`, the same exact version yog pins, so exactly ONE brazen resolves.'

Index line: {"name":"lernie","vers":"0.0.3","deps":[{"name":"brazen","req":"=0.0.5",...}],"yanked":false,"rust_version":"1.88","pubtime":"2026-07-30T07:37:53Z"}

Shipped in 0.0.3: the compaction-recursion fix [bl-a9eb], the worktree descriptor-grant fix [bl-18a9], the shipped-template role grant fix [bl-38c2], and the brazen pin bump [bl-e4ef]. yog can now pin lernie =0.0.3 + brazen =0.0.5.

Note for future releases: the release PR's CI arrives as `action_required` (workflow approval pending), which shows as mergeStateStatus UNSTABLE with 'no checks reported'. It is not a failure — approve the run (`gh api -X POST repos/mudbungie/lernie/actions/runs/<id>/approve`) and it goes CLEAN.