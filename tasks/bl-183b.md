+++
title = "re-vendor the wire corpus: reply/follow gained the tool window and reply/ops gained the client that asked"
created = 1788675541
updated = 1788746268
claimant = "Cantaloups-S5"
priority = 2
root_commit = "3efc0d263898c425a0ff2bb042938233e838f436"
tags = ["usability-r1"]
+++
yog round-1 fix lane Y5 moved two wire-visible shapes. lernie vendors `corpus/`, so this is the re-vendor plus the two seat-side renderings the new fields exist for.

REMOTE §3 is the protocol authority and yog `src/wire/hello.rs` carries the version; the two moves are recorded at REMOTE §5.5 and §9.18.

## 1. `reply/follow` gained `tools` (yog bl-5305)

The follow lane carried the model's prose alone, so an operator watching an
agent administer their machines saw a thinking marker and two sentences while
eight commands ran on two boxes. The frame body now carries the **tool
window** beside the fold, appended by the same rule the prose obeys —
concatenate the lists of a read's frames in order:

    {"tool_use": "toolu_01", "tool": "box2_Bash",
     "input": "{\"command\":\"hostname && uptime\"}"}
    {"tool_use": "toolu_01", "exit_code": 0}

- Two entries per call and both are transitions: `input.json` lands before the
  call is dispatched, `output.json` when the capture returns.
- `exit_code`'s **presence** is the status. Absent is a call in flight.
- The closing entry restates neither name nor input — key it by `tool_use`.
- The machine a routed call ran on is already in `tool`: REMOTE §5.1 presents a
  loaded remote tool as `<client>_<tool>`, always.
- `tools` is **required**, empty list included.

The lane's liveness also widened: a follow read now follows a STEP rather than
the model call inside it, so it stays open through the tool phase instead of
terminating the instant a call settles into its `tool_use` blocks.

## 2. `reply/ops` rows gained `client` (yog bl-e59e, round-1 ruling 5)

Every trail row now names the identity that made the act — a connection's
certificate common name, or `local` for the window, the gestures inbox and
yog's own loops. Read strictly, like `standing`: it is the second field a
reader cannot recompute from the line beside it.

## What this ball asks of lernie

1. Re-vendor `corpus/` from yog and make the conformance replay pass on both
   shapes, in both directions.
2. `lernie follow` renders the tool window: what is running, where, and what it
   came back with. Prose-only output is the defect this closes.
3. The ops rendering names the author.
4. **`lernie help follow` is wrong today** and yog bl-5305 filed it as the
   second half of that ball: it states "Each frame is the WHOLE accumulated
   fold rather than a delta, so a frame missed is nothing missed." REMOTE §5.5
   has said the opposite since bl-3655 — a frame carries what landed since the
   read's previous frame, and a follower absorbs them in order onto an empty
   fold. Either the help text or the frames were wrong, and it is the help
   text.

---

Round 2 lane swdev: this is no longer one version behind and no longer p2.

Installed lernie 0.1.34 and the tree at 1763302 both carry `pub const PROTOCOL: u32 = 13` (src/channel/hello.rs:165). Installed yog 0.0.49 speaks 15; yog main 5cd95986 speaks 16. So the RELEASED seat cannot dial the RELEASED engine:

    lernie workspaces
    (this box's own engine)
        wire protocol mismatch: this seat speaks version 13, the engine speaks 15.
        There is no negotiation — upgrade the older component until both speak one version.

Three bumps landed on yog during the round-1 fix wave: 13→14 (yog bl-fec6), 14→15 (yog bl-5305), 15→16 (yog bl-e59e). This ball's body names only the first of them (reply/follow's tool window, reply/ops' client), so the re-vendor is now a three-version catch-up.

This lane proceeded by copying the tree aside, raising the constant to 16 and building — which reaches the engine but cannot RENDER the new kinds. Verbatim, on a /config write that succeeded:

    this seat cannot read that answer: reply: this seat cannot paint a "applied" answer — the engine speaks a kind this build does not; upgrade the seat (--json prints the frame)

That is the honest half working: the seat says which half it cannot do rather than mis-painting. Everything in this lane past that point used --json.

The foot half of the same fact is thrall bl-cef6 (pin 13 vs engine 16, the fourth recurrence of bl-e0f0/bl-f88f/bl-dc5f), which also argues for deriving the pin from yog's corpus rather than copying it.

---

Item 2's rendering has a frame waiting for it. bl-293d landed the column the
activity rows go in, and left the rows themselves here because they need the
decode this ball owns — a decoder judged by fixtures it wrote itself is what
the vendored corpus exists to prevent.

**What is already there** (`src/render/parts.rs`, `src/render/walls.rs`):

- `GUTTER` (`┊`) and `narrated(&str)` — the one spelling of *a line the seat
  wrote about the run*, as against a line the work produced. Every line of a
  multi-line narration keeps the mark.
- `walls::follow`'s heartbeat wears it; the model's prose keeps the left
  margin, deliberately unmarked.
- `walls::at_rest` is the closing line and already carries the elapsed. It
  wants the **count of calls** beside it, which is the one field this ball's
  decode makes countable.
- DESIGN §4.37 records the rule and names this ball as where `tools` lands.

**What item 2 then adds**, per bl-293d's shape: one `narrated` row a
transition, beside the heartbeat —

    ┊ box2     bash · systemctl restart nginx           …
    ┊ box2     bash · systemctl restart nginx        0  1.2s

- column 1 the tool, a routed call split on `<client>_<tool>` (REMOTE §5.1
  guarantees the shape) with the machine first, because on this lane the
  question is which box it is happening on;
- column 2 the subject, elided by `parts::brief` so the mark is never silent;
- column 3 the exit code, printed only once `exit_code` is present — its
  presence IS the status, so a live call needs no spinner and the trailing `…`
  is enough;
- column 4 the elapsed between the two transitions' arrival.

Item 4 (the `follow` help text) is untouched and still wrong on today's tree.
