+++
title = "ARCH §3.3 and multi.rs cite a tracking ball that no longer exists: bl-a690 closed unbuilt"
created = 1785995956
updated = 1785995956
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
bl-a690 (read/write parallel split inside the multi_tool envelope) was closed unbuilt on 2026-08-06 — the deferral condition never fired, and the ball's own note records why. Two in-tree citations still assert the deferral is tracked:

- docs/ARCHITECTURE.md §3.3 line ~670: '...deferred until the serial rung proves too slow (tracked, bl-a690): adopting it costs a per-tool read/write classification...'
- src/prompt/dispatch/tool_step/multi.rs line ~29: '...a read/write parallel split inside it is deferred (bl tracked, see ARCH §3.3 *The multi-tool*).'

The design reasoning in both is still correct and stays. Only the tracking claim is stale: drop 'tracked, bl-a690' from ARCH and 'bl tracked' from multi.rs, leaving the deferral stated as a standing design position rather than a queued task. No behaviour change.