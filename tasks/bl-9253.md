+++
title = "Cut 0.0.1: push main, drive release-plz to a tagged crates.io publish"
created = 1784955403
updated = 1785124306
claimant = "Halyard"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
BLOCKED on bl-53e3. State as of 2026-07-26, Halyard.

WHAT LANDED
- Stale claim (Gantry) released; claimed by Halyard.
- PUSHED main: e3123ba..39c10a5, then 31cb7d0..0a6a36a. origin/main == local main (0/0).
- CI ON MAIN WENT GREEN, FIRST PASS (run 30216420512, commit 39c10a5, 10m52s, 'make ci' clean). BLOCKER 1 of the previous attempt is dead: cbdc9d4 [bl-636b] + 1588f1c [bl-8366] resolve the brazen pin from Cargo.toml (make brazen-pin) and pin-cache both bz homes. No flake — spawn_retries_past_transient_etxtbsy did not recur.
- BLOCKER 2 (CARGO_REGISTRY_TOKEN) is dead: f831a89 [bl-6cfb] moved publishing to crates.io trusted publishing via OIDC. 'gh secret list' is empty and that is CORRECT. Trusted publishing is bound to workflow file .github/workflows/release-plz.yml — NEVER rename it.
- PR #1 'chore: release v0.0.1' is MERGED. Held bot workflow run 30216460458 approved via 'gh api --method POST /repos/mudbungie/lernie/actions/runs/30216460458/approve'; its CI went green; diff verified CHANGELOG-only (+205/-0, new file, version 0.0.1, Cargo.toml untouched) immediately before merging. Merge commit 31cb7d0; branch release-plz-2026-07-25T00-17-04Z deleted. CHANGELOG.md is now on main.

NEW BLOCKER 3 — 'release-plz release' aborts before publishing. FILED AS bl-53e3 (p9).
Release-plz fired correctly on the green CI (workflow_run run 30216800926) and the 'release-plz release' job FAILED in 23s at the 'Run release-plz' step:
    ERROR Package `agent-eval` has `publish = false` or `publish = []` in the Cargo.toml, but it has `publish = true` in the release-plz configuration.
Hard abort of the whole release command — lernie itself never got published.
CAUSE: crates/agent-eval/Cargo.toml has `publish = false` (a936923 [bl-8801]); release-plz.toml's agent-eval block sets only `release = false`. release-plz's per-package `publish` key defaults to true and is validated INDEPENDENTLY of `release`, so the two files disagree.
FIX (config only, one line): add `publish = false` alongside `release = false` in release-plz.toml's `[[package]] name = "agent-eval"` block.
Why it was never caught: the 'release-plz PR' job succeeds (run 30216420496) — release-pr skips this consistency check; only `release-plz release` runs it, and the release job had never reached that point before because CI was red.

END STATE — 0.0.1 IS NOT RELEASED
- crates.io: lernie max_version 0.0.0; versions ['0.0.0'] only.
- origin tags: NONE ('git ls-remote --tags origin' is empty — note the changelog's v0.0.0...v0.0.1 compare link points at a tag that does not exist; cosmetic).
- GitHub Releases: none ('gh release list' empty).
- No binary artifact attached; the 'lernie x86_64-unknown-linux-gnu' job never ran.

REMAINING PATH
1. Land bl-53e3 (release-plz.toml publish=false for agent-eval) via a worktree; push main.
2. Green CI on main re-fires release-plz via workflow_run; 'release-plz release' should then cargo-publish 0.0.1 over OIDC, tag v0.0.1, cut the GitHub Release from CHANGELOG.md (now in tree), and attach the linux-gnu archive.
NOT CLOSING until crates.io shows lernie 0.0.1.