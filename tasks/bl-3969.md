+++
title = "consume yog's lost-reply recovery contract: an act with no reply is in doubt — never auto-resend"
created = 1788402522
updated = 1788402522
priority = 2
root_commit = "3efc0d263898c425a0ff2bb042938233e838f436"
tags = ["boundary", "wire"]
+++
yog bl-d1f1 stated the wire's lost-reply recovery contract in REMOTE §3, as a
new bullet ahead of "The disk inbox survives for in-world callers":

> A lost reply leaves an act IN DOUBT, and the recovery is a read — never a
> resend. [...] a client whose act earned a transport error instead of a
> reply paints the failure and consults the world, which is the durable
> record (§9.8: the ops.jsonl trail, the transcript, the roster — the reads
> are the recovery). Asks are the opposite case and re-ask freely.

Nothing wire-visible moved — no envelope field, no verb, no reply kind —
so PROTOCOL stands at 8 and the corpus is unchanged; there is nothing to
re-vendor. What the seat owes the contract is behavioral:

1. Verify the poster never auto-resends an act after a transport error. The
   §9.8 receipt discipline (every ticket earns exactly one receipt; a refusal
   keeps the draft with the reason) already implies this — confirm no retry
   loop exists on the act path, and add the cheap regression if none names it.
2. The painted failure for a connection that died mid-act should read as *in
   doubt*, not as *failed*: the engine may have completed the act. The
   transcript/roster the seat already re-reads is the recovery, which is the
   contract's point — no new mechanism.
3. Reads are exempt: a standing question may re-ask freely (yog REMOTE §9.7).

Context: yog's disk inbox got the mechanical half (a claim is lock-held; a
dead claimant's gesture is answered in doubt on its reply slot at the next
engine boot — yog DESIGN §8.5). The wire has no reply slot, so the seat's
half is exactly the behavior above.