+++
title = "the window: the egui face, and the paint layer that can testify about it"
created = 1788068281
updated = 1788069101
claimant = "OrderGlazier"
priority = 4
root_commit = "3efc0d263898c425a0ff2bb042938233e838f436"

[[blockers]]
id = "bl-4174"
on = "claim"
+++
The seat exists to be a window. This is that half.

What travels from yog: the shell and its panes, the theme, the transcript and its row layers, and the test infrastructure that makes any of it assertable — the paint probe and the rule that governs it. That rule travels because the defect it exists to catch is not a yog defect: a galley reports the string that went IN, so a label egui elided to an ellipsis still reads back whole and every assertion against it is blind to truncation. Upstream found that three times before it became a rule. It arrives here as a rule, not as a memory.

It also brings the largest dependency approval this crate will make (eframe/egui), which is why the manifest does not pre-grant it: the approval lands with the ball that links it, and that ball re-derives the licence allow-list from the lockfile it produces.

Gated on the reply vocabulary, which is what a window paints from.