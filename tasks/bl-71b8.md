+++
title = "Papercuts: read_file oversize reports fabricated size; lernie new on an unseeded data root drops descriptions/ silently"
created = 1784955704
updated = 1784955704
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Foxglove findings 13+14. (13) read_file rejects >1MiB correctly but always reports 'is 1048577 bytes' (cap+1 — the capped read's length) for ANY oversize file; stat the file and report the true size. (14) lernie new against an unseeded data root exits 0 with a config commit carrying NO descriptions/ at all — violating §3.3 descriptions-always; agents forked off it see no tool descriptions. Fix per severability/general-path: lernie new should found the pools via the same seed-if-absent routine prime uses (prime is idempotent — reuse the routine, no new flag), so the general path has no unseeded special case; if ARCH forbids new touching the harness root, refuse actionably instead ('run lernie prime first'). Read ARCH §2.2/§3.3, pick, align docs. Tests both.