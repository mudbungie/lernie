+++
title = "agent worktrees carry descriptors for tools the role cannot call: descriptions/** is snapshotted and inherited unfiltered by the grant"
created = 1785287931
updated = 1785288328
claimant = "scorched-lernie-tools"
priority = 5
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
tags = ["defect"]
+++
## Origin

Filed from yog's store, **yog bl-55b1** (investigation, two independent
reproductions on 2026-07-28: conversations `20260728T072256Z-385dcda9` and
`20260728T080619Z-3b14deaf`). Sibling of bl-38c2 (yog bl-bd9d); that one is the
grant, this one is the visibility.

## The defect

`lernie new` snapshots the **whole** data-root pool into the config commit's
`descriptions/**` (ARCH §3.3 *Descriptions-always population*), and the dispatch
commit (§2.3 step 2) strips the control files but deliberately leaves
`descriptions/**` in place. So every agent branch carries schemas and skill
blurbs for all five shipped tools regardless of what its role's `tools:` grants.

The wire array, by contrast, is the intersection of the grant with the snapshot
(`src/prompt/dispatch/tools.rs::compose`). The agent therefore has strictly more
filesystem visibility than capability, and nothing on its branch distinguishes
the two: `providers.yaml` — the actual grant — is removed from the agent's tree
by the dispatch commit and lives only on `config/*`.

Observed cost, twice: asked "why can't you send a message?", the agent
`bash`-explored `descriptions/`, found `descriptions/tools/message.json` plus
`descriptions/skills/message.md`, concluded (messages/010) *"this environment
seems to support messaging"*, and only after ~8 more steps landed on the true
answer (messages/018): *"only bash, read_file, and load_skill are actually
exposed to me as callable tools here."*

## It also breaks a stated invariant

ARCH §5.1: *"Everything inside a branch's worktree is composed into that agent's
prompt. There is no exclusion list, no filter, no 'this path is for the harness
only.'"*

A non-granted tool's descriptors compose **nowhere**: `assembler/body.rs` skips
`descriptions/tools/**` and every tool-claimed skill description (`tool_backed`,
per §3.3's *two wire homes*), and the tools array does not carry them because
the role did not declare them. They are worktree content with no wire home —
exactly the exclusion §5.1 says does not exist — reachable only by `bash`.

## Deliverable

Make what an agent can **see** equal what it can **call**, with no new artifact:
extend the dispatch commit's existing tree-trim so it prunes
`descriptions/tools/<name>.json` (and the tool-claimed
`descriptions/skills/<name>.md` beside it) for every tool the forking role does
not grant. Standalone skills — those no tool claims — stay: they compose as head
text and are `load_skill`-able.

One predicate, at the one place the tree is already trimmed
(`step_commit::remove_control_files`, called by both the root dispatch commit and
`subagent::spawn_subagent_branch`). No second source of truth for the grant, so
nothing can drift from it. `ls descriptions/tools/` becomes the one-step answer
to "what can I call?".

Rejected alternative: committing a resolved-grant file (`role.json`) into the
worktree. It leaves the misleading descriptors in place and creates a second
representation of one fact — the drift PRINCIPLES bans.

Known consequence, spec-sanctioned: a compactor (`tools:` empty) loses the
verbatim schemas its inherited transcript names, so §3.3's closure stands in a
bare `{"type":"object"}` — *"the entry exists to make the history legible, not
to offer the tool."* `src/e2e/compaction_wire.rs` asserts the name is declared,
not the schema body.

## Gates (AGENTS.md)

- **tests** — `make check`: fmt, clippy -D warnings, tarpaulin --fail-under 100,
  install contract.
- **docs** — ARCH §3.3 / §2.3 step 2 / §2.2 record that the trim is
  grant-scoped, and `workspace::CONTROL_PATHS`' "descriptions/** stays" comment
  corrected.
- **alignment** — ARCH §5.1 (worktree invariant), §3.3 (two wire homes,
  descriptions-always), §4.3 (toolset), PRINCIPLES (single source of truth).

## Landed

The dispatch commit now trims the forked tree to the role's grant. One
predicate, at the one place the tree was already being trimmed; no new
artifact, no second copy of the grant.

- `src/prompt/dispatch/step_commit/descriptors.rs` (new) —
  `prune_ungranted(worktree, granted, git)`: stages `git rm -q
  --ignore-unmatch` of `descriptions/tools/<name>.json` and its
  tool-claimed `descriptions/skills/<name>.md` for every schema in the
  tree the grant does not name. Issues **no** git command when there is
  nothing to prune, which is the shipped default's case (worker grants
  the whole pool, bl-38c2) and what makes it idempotent across a fork off
  an already-pruned parent. A standalone skill is never touched.
- `src/prompt/dispatch/step_commit.rs` — `remove_control_files` became
  `trim_to_context(worktree, granted, git)`: control files out, then the
  prune. Both call sites already invoked the former.
- `src/prompt/dispatch/mod.rs` — the root dispatch commit passes
  `resolved.tools`, the grant the role actually resolved.
- `src/prompt/subagent/mod.rs` — `SpawnRequest` carries `granted`.
- `src/prompt/child_dispatch.rs` — reads the child role's `tools:` from
  the **same** governing config commit the soul comes from. A role with no
  `tools:` grants none (§4.3's own reading; the compactor's shape).
- Docs: ARCH §3.3 gained *The fork prunes the snapshot to the role's
  grant* (the §5.1 argument, the reproduced failure, the compactor
  consequence, and why no `role.json` is written); §2.3 step 2 restated as
  a trim; `workspace::CONTROL_PATHS`' comment corrected.

### Tests

- `dispatch::step_commit::descriptors::tests` (7):
  `a_grant_covering_the_whole_snapshot_issues_no_git_op`,
  `ungranted_tools_lose_schema_and_claimed_skill_together`,
  `an_empty_grant_prunes_the_whole_snapshot`,
  `a_tree_with_no_snapshot_is_a_no_op`,
  `non_schema_entries_in_the_tools_dir_are_ignored`,
  `an_unreadable_snapshot_dir_surfaces_rather_than_pruning_blind`,
  `a_failing_git_rm_surfaces_as_a_named_git_error`.
- `prompt::tests::descriptor_prune::the_dispatch_commit_stages_the_ungranted_descriptors_for_removal`
  — the root wiring: the fixture grants `[bash, read_file]`, the inherited
  tree carries a `message` pair, and exactly that pair is staged.
- `prompt::child_dispatch::tests_grant::the_childs_tree_carries_only_the_descriptors_its_role_grants`
  — the whole defect end to end over real git: `git ls-tree` on the child
  branch shows `bash`'s pair and the standalone skill, no `message` pair,
  no `providers.yaml`.
- `prompt::child_dispatch::tests_grant::missing_providers_is_surfaced_as_control_read_before_any_spawn`.

`child_dispatch/tests.rs` was split (`tests_grant.rs`) and `descriptors`
nested under `step_commit` to stay inside the 300-line cap.

### Gate

`make check` green: fmt-check, clippy `-D warnings`, tarpaulin
**100.00% (5177/5177)**, install contract.