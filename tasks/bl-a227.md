+++
title = "README compaction + dispatch narrative describes deleted code"
created = 1784955704
updated = 1784955704
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Foxglove findings 7+8 (docs). (7) README's entire terminal-compaction story is stale: 'On a normal completion, dispatch the terminal compactor … lands the compaction merge … the compactor is a stub — it does not call a model', plus the copy-pasteable inspection commands (show --stat merge commit; show agents/<id>:summary/001.md) all FAIL against shipped behavior. src/prompt/dispatch/terminal.rs:10 says outright: 'There is no terminal compaction … the v0.3 compactor dispatch … is deleted'; docs/ARCHITECTURE.md:324 documents the real design (compactor is an ordinary child, real model call via bz, merge via compactor_return at configured checkpoints). Rewrite the README sections (Sending a prompt step 8, dispatch compactor, inspection commands) against ARCH's current §2.7 and verify every example command actually works. (8) The 'children do not run a step loop / worker.rs stops at the dispatch commit' limitation is FALSE now — a live child ran a full step loop and deposited a final-response epitaph; purge that claim from README (dispatch tool bullet, scan section, dispatch worker section) and the stale doc-comment in src/prompt/dispatch/result_deposit.rs. Also add one honest note: the local ollama provider row rejects tool_result blocks ('user accepts only text content'), so the SMOKE_PROVIDER=local recipe validates the tool-free path only (brazen-side limitation, ball filed there). Verify every remaining README claim you touch by running it.