+++
title = "Crates.io packaging: metadata, exclude repo-development files, resolve the eval island"
created = 1784955529
updated = 1784955529
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Harrow audit findings 3+4+5. (a) Cargo.toml lacks keywords, categories, rust-version (edition 2024 needs rustc>=1.85; without MSRV old-Rust users get an incomprehensible edition error instead of cargo's clean message). Add rust-version=1.85, keywords, categories. (b) cargo package ships 275 files incl. rust-toolchain.toml (force-pins source builders to 1.95.0 — confirmed rustup honors it from the extracted dir; cargo install unaffected via RUSTUP_TOOLCHAIN env), .balls/, .githooks/, .github/, AGENTS.md, CLAUDE.md, Makefile, tarpaulin.toml, release-plz.toml, scripts/, docs/ (396KB), experiments/, tests/suite/. Add exclude=[...]. VERIFY after excluding: cargo package verify-build must stay green — the include_dir! trio (template/, schemas/tools/, skills/) and install/models.yaml must stay in. (c) The eval island: crates/agent-eval (~1200 LOC, publish=false) defaults --agent to lernie-eval-agent, a binary that EXISTS NOWHERE — make eval fails at spawn; LERNIE_EVAL_REPORT contract undocumented; tests/suite.rs:4 says runner deferred while tests/suite/README.md:11 says shipped (former is honest). Decision (orchestrator, per minimalism): do NOT write the driver for 0.0.1 — exclude the island from the tarball, make make-eval/docs honest about the missing driver, reconcile the two contradictory suite docs, and file a deferred ball for the lernie-eval-agent driver.