+++
title = "DESIGN §4.12 does not exist, and the module map is missing the threads it names"
created = 1788138455
updated = 1788138455
priority = 2
root_commit = "3efc0d263898c425a0ff2bb042938233e838f436"
+++
Four places in `docs/DESIGN.md` cite **§4.12** — the summary at the top ("three threads (§4.12) — the asker over the standing question set, the poster draining what a click composed, and the follow lane"), §4.11 ("What fills the model is §4.12"), §4.11 again ("the standing set is derived (§4.12)"), and §5's confinement table ("occupied, by the tenant the rule was written for (§4.12)"). **Section 4 ends at 4.11.** The commit that introduced the threads wrote the references and never wrote the section.

The same commit left §5's module map without a row for anything it added: `src/state.rs`, `src/state/traffic.rs`, `src/offframe.rs`, `src/offframe/asker.rs`, `src/offframe/poster.rs`, `src/offframe/follow.rs`, `src/seat/holds.rs`, `src/test_support/window.rs`, `src/test_support/wire.rs`. The map is the doc's inventory of the tree; nine files outside it means the inventory is not one.

So this ball is two edits and one rule: write §4.12 — what the three threads are, the standing set as a derived query, and the one lock the rule named before there was anything in it — and add the missing rows with their cap bands. The authority is not allowed to name a section it does not have.