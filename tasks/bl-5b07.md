+++
title = "PROTOCOL 2: consume the two moved wire shapes — the advertised element's optional subject_cwd and the invocation family's optional cwd"
created = 1788151374
updated = 1788151374
priority = 8
root_commit = "3efc0d263898c425a0ff2bb042938233e838f436"
+++
yog bl-77be (with thrall bl-36f7) bumped the wire to PROTOCOL 2 — the corpus
ledger forced it for four shapes: request/advertise, request/invoke,
reply/clients, reply/invocations. Two facts moved, both optional fields:

- the advertised element gained `"subject_cwd": true` (REMOTE §5.1 as
  amended): a box's per-tool consent to execute at a working directory the
  invocation names — the worktree lane's routing fact;
- `invoke` and the `invocations` row gained `"cwd"` (REMOTE §5.3): the
  subject's location, set by the lane, honoured only under that consent.

Until the seat speaks 2 it is refused at the preface, fail-closed, both
versions named — the designed skew behavior, not a defect. The work here:
bump the seat's PROTOCOL constant in lockstep, re-vendor/replay yog's
regenerated corpus, decode the two optional fields (absent reads
false/None; strict on type), and surface `subject_cwd` wherever the seat
renders a client's advertised tools so an operator can see which entries
consent. yog's docs/REMOTE.md §5.1–§5.4 is the authority.