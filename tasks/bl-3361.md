+++
title = "bare 'per-call' for the tool disk record survives tree-wide — ~15 more sites of the sense ARCH §2.1 bans"
created = 1785996591
updated = 1785996591
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
bl-7173 fixed the three `cd`-working-directory sites (`docs/ARCHITECTURE.md` §3.3, `src/install/tests/toolspec.rs` ×2) to "per-tool-call". The same banned sense survives elsewhere, mostly as "the per-call disk record" — the record written per *tool call* under `<step_dir>/tools/<tool-id>/`.

ARCH §2.1: **"call"** without a qualifier, *as a term for an interaction with a model, tool, or API* — use model call, tool call, or API call. The bl-1966 carve-out sanctions the programming sense only.

Sites (tool-call sense → "per-tool-call"):
- `docs/ARCHITECTURE.md:213` ("the per-call `output.json`"), `:1296` ("the per-call disk record")
- `docs/USER_STORIES.md:175` ("a per-call diagnostic record")
- `README.md:519` ("the per-call records land under")
- `src/prompt/tool/mod.rs:6,9,71,81,197`
- `src/prompt/tool/spawn.rs:3,39`
- `src/prompt/dispatch/tool_step.rs:14`
- `src/prompt/tests/tool_stub.rs:6`, `src/prompt/tool/tests/bash_tool.rs:9`, `src/prompt/tool/tests/read_file_tool.rs:12`

Model-call sense → "per-model-call":
- `src/prompt/adapter.rs:43` ("never a per-call value the harness")
- `docs/DESIGN_AGENT_ATTESTATION.md:102` ("assembly is stateless and per-call")
- `docs/TAXONOMY.md:814` ("hidden output tokens per call")

Leave alone (programming sense, sanctioned):
- `src/harness_root.rs:21`, `src/prompt/stop/tests/fixtures.rs:31`, `src/prompt/tool/mod.rs:222` ("Passed per call rather than…" — a function parameter)

Also leave `CHANGELOG.md:248` (historical entry, verbatim record) and `docs/TAXONOMY.md:665` (`LLMTestCase` = deepeval's own term, quoted).