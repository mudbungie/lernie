+++
title = "Doc/terminology batch: session POSIX carve-out, README drift, test-double calls renames"
created = 1784955529
updated = 1784959260
claimant = "Kestrel"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Harrow audit findings 6+8+9 (doc/terminology batch). STATUS 2026-07-24 ~21:45: all three batches IMPLEMENTED and staged in worktree /home/u/.local/state/balls/plugins/bl-delivery/home/u/dev/lernie/bl-81a0 (branch work/bl-81a0), NOT yet committed — the pre-commit gate keeps dying on environmental flakes, not on this diff.

(6) DONE. docs/ARCHITECTURE.md:75 now reads: "**\"session\"** — underdefined, per-framework overloaded, and colliding with the transport/connection sense (`docs/TAXONOMY.md` §3). Stays banned as an interaction-span term; its only sanctioned use here is the POSIX process-session sense — the `setsid` session a detached spawn leads (§2.11)." docs/TAXONOMY.md:281 mirrored: "**session** stays **banned** as an interaction-span term (…), the POSIX process-session sense (`setsid`) excepted." The 5 legitimate POSIX uses (README:555, src/prompt/inbox/mod.rs:125,161, ARCH:377,411) are untouched. AGENTS.md/PRINCIPLES.md banned-term lists left alone (they derive; ARCH §2.1 is the single source).

(8) DONE (3 of 4 items). README `make coverage` row now names the hard TARPAULIN_PIN 0.35.2 gate (Makefile:50) with the fix-it install line; new `make eval CONFIG=… SUITE=… RUNS=…` row added to the build-target table; `make install` row now lists "an empty `workflows/` templates dir" under the config root (matches README:57 and src/install.rs:69). SKIPPED deliberately: LERNIE_EVAL_REPORT documentation — owned by bl-8801 (Junction), which is rewriting the eval island prose and may delete `make eval` outright; reconcile at merge. bl-8801 had NOT landed as of this writing.

(9) DONE. All identifiers literally named `calls` renamed to `invocations` across src/, tests/, crates/ (22 files: inbox/scan/tests/{mod,flush,sweep}.rs, inbox/tests/probe.rs, child_dispatch/tests.rs, tests/{tool_stub,multi_step,multi_step_terminal,advance,advance_edges,exit_launch,happy,stop_deposit,verifier_gate}.rs, tool/builtin/dispatch/tests/{fixtures,happy}.rs, tool/builtin/message/tests.rs, compactor/merge/tests.rs, dispatch/transfer/tests.rs, stop/{cascade,inspector}.rs, crates/agent-eval/tests/runner.rs). Plus two bare-noun doc comments reworded: assembler/body.rs:87 ("spans calls"→"spans invocations") and tool/builtin/bash/mod.rs:214 ("repeated calls"→"repeated invocations"), and tool_stub.rs:25 "per call"→"per tool call". NOT renamed (judged compliant, do not redo): verb uses of "calls" (adapter.rs:62, subagent/*, compactor/checkpoint.rs:17, driver.rs:23, stop/discover:21, inspector.rs:74, cmd/tests/surface.rs, template/*, agent-eval/src/agent.rs:3, tests/suite/hallucinated_apis.yaml:8); qualified nouns `tool_calls`/"`tool_use` calls" (multi_step.rs:76, transcript.rs:97, hop.rs:182), "`open` calls" (inbox/lock.rs:13); the type alias ObservedCall and `call:` bindings whose type is ToolCall/ModelCall (the type supplies the qualifier). No §2.1 test-double exemption was added, per orchestrator decision.

VERIFICATION: `cargo fmt`, `cargo clippy --all-targets -D warnings`, `cargo test --workspace` (834 passed / 0 failed) all green in the worktree. Standalone `make coverage` reached 100% (no partially-covered files).

BLOCKERS HIT (all environmental, all already ticketed):
- bz drift: machine `bz` is 0.0.4, tree pins brazen "=0.0.3" → 5 e2e tests (advance_cli, prompt_end_to_end, prompt_retry, stop_cli x2) fail the version guard. Ball bl-8c92 (Umber) bumps the pin; bl-8366 names the PATH race. WORKAROUND IN PLACE, use it: a private brazen 0.0.3 is installed at /tmp/claude-1000/-home-mark-dev-lernie/fe8c0ced-96b4-45ac-8547-30d9edb8e1e3/scratchpad/bzroot/bin (NOTE: bzroot, not bz003). Commit with `PATH=<that dir>:$PATH git commit -F <msg>` and those 5 pass. If the scratchpad is gone: `cargo install brazen --version =0.0.3 --locked --root <dir>`. Once bl-8c92 lands on main, merge main and drop the workaround.
- bl-7a3f/bl-2061/bl-9300: `prompt::tool::tests::errors::spawn_retries_past_transient_etxtbsy` fails whenever the box is loaded (40ms hold vs retry budget) — failed 3 gate runs.
- bl-5f0c: stop-cascade pgid discovery SIGTERMs the gate runner ("pre-commit died of signal 15") — killed 3 gate runs mid-tarpaulin. Mitigation: run each commit attempt under its own session (`setsid --wait …`) so the cascade cannot reach the supervisor.

REMAINING STEPS TO CLOSE: (1) cd into the worktree, `git merge main --no-edit` (main advanced to 803aa00; reconcile with bl-8801 if it landed eval/README prose — if it added the `make eval` row, drop mine). (2) Commit the staged diff with the PATH-pinned bz recipe above; retry until the gate is green (an auto-retrying supervisor script lives at <scratchpad>/autocommit.sh; commit message draft at <scratchpad>/msg.txt). NO --no-verify, ever. (3) From /home/u/dev/lernie: `bl close bl-81a0 --as Kestrel -m "…"`. (4) Sweep the three gate children (gate: tests / gate: docs / gate: alignment, parent bl-81a0) via `bl list --json`, verify each, close each.