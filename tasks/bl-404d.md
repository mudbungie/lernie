+++
title = "the mint moves in: every creation path auto-mints a one-word name on omission; the dispatch parameter is taught"
created = 1785737040
updated = 1785737120
claimant = "mint-mover"
priority = 2
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Operator ruling (yog bl-aca4, 2026-08-03): name is an exposed dispatch parameter; omission auto-mints a one-word name. The mint's one home is ruled to be lernie — yog DESIGN §3.3 (bl-aca4 amendment) records the ruling, the rivals, and why they lost. This ball is the lernie half; yog's consume ball is gated on it.

What moves in (from yog src/names — mint half only):
- The wordlist (`words.txt`, ~7.4k words, EFF-derived; the attribution/licence header MUST ride along — it is the CC BY 4.0 attribution and ships in the binary via include_str!).
- `mint(rng, occupied) -> Result<String, Exhausted>`: one RNG draw picks a start index, wraparound scan discards occupied words, one full lap proves exhaustion. Pure over an injected RNG trait + occupied set. Keep it list-injectable so tests drive collision/exhaustion on a tiny pool.
- The `Rng` trait + a seedable production impl (yog uses SplitMix64, ~6 lines, zero deps — bring it or equivalent). Tests script the draw; production seeds from entropy.

Home: beside the uniqueness check it races — `workspace/agent_name`. Add a settle-the-name pre-flight seam: supplied name -> `require_available`; absent -> mint against the same living-names scan (`agent_name::named()`) uniqueness reads. Occupied set stays one derivation.

Wire it at BOTH creation pre-flights (verified in 0.0.5):
- `src/prompt/mod.rs:148` (`lernie prompt`) — currently `name.map_or(Ok(()), |n| require_available(...))`; absent must now mint.
- `src/prompt/child_dispatch.rs:96` (`lernie dispatch`, which the `dispatch` tool shells into) — same.
No fork may end nameless. `settle` then always writes a real name for new agents; empty-blob reads stay lawful (pre-existing unnamed agents age out via retention). Unnamed becomes readable-but-not-creatable.

Tool description teaches the parameter (`schemas/tools/dispatch.json`, `name`): the operator's stated purpose is identity clarity in subagent trees — say what a name buys (message-addressable, tree-readable child) AND that omission mints a one-word name silently and validly. Keep `required: [role, goal]`.

Public API: export `mint` + the Rng trait + the production RNG from the crate — yog consumes them through its existing `lernie = "=0.0.5"` link (next pin) for its composer preview and fire. The wordlist itself is NOT an exported surface; the interface is the function.

Needs a lernie release: yog's consume ball (in the yog store, gated on yog bl-aca4) must not start until the published crate verifiably carries this. Cut a release after landing.

Verify premises against the tree before editing — line numbers are from pinned 0.0.5 and may drift.