+++
title = "Coverage: tarpaulin's llvm engine merges profraws from two different binaries by function name (order-dependent counter attribution)"
created = 1784955880
updated = 1784955880
priority = 2
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Split from bl-1c2e, which reproduced and fixed a *different* cause of the same
symptom (a wall-clock retry deadline making one line's coverage load-dependent,
src/prompt/tests/advance.rs). This ball carries the lead bl-1c2e could not close:
the `src/prompt/budget/mod.rs:127` sighting, which has no timing component.

## The hazard (read from tarpaulin 0.35.2 + llvm-profparser sources)

Tarpaulin's llvm engine sets `LLVM_PROFILE_FILE=<target>/tarpaulin/profraws/<test-binary>_%m-%p.profraw`
for each test binary (`src/process_handling/mod.rs:273`). Every subprocess a test
spawns **inherits that variable**, so the instrumented `lernie` binary our e2e
tests drive writes its own profraws into the same pool. After the test binary
exits, tarpaulin merges every new profraw in that pool
(`src/statemachine/instrumented.rs`, `merge_profiles`) and maps the result
through the *test binary's* coverage map alone.

The merge is keyed by function NAME, not by the structural hash:

- `InstrumentationProfile::merge_record` looks the record up with
  `find_record_by_name_mut` (llvm-profparser `src/instrumentation_profile/types.rs`);
- `InstrProfRecord::merge` starts with `if self.counts.len() != other.counts.len() { return; }`
  — a same-named record whose counter vector differs in length is silently
  discarded, no warning;
- `CoverageMapping::get_simple_counters` binds a function's regions to
  `find_record_by_hash` → symtab name → `find_record_by_name`, i.e. by name again.

Whichever profraw `merge_profiles` parses FIRST becomes the base record for a
name. The parse order is the `WalkDir` order of the profraw directory, and the
filenames carry `%p` (pid), so the order changes from run to run with no code
change. If a `lernie`-binary record wins the base slot for a function whose
test-binary record has a different counter layout, the test binary's own
in-process counts for that function are dropped and its regions are read off the
wrong counter vector — a line a unit test provably executes reports uncovered,
and an immediate retry greens.

## What to do

1. Confirm the hazard is live here: after a run, `llvm-profdata show --all-functions`
   the pool's test-binary profraw and a `lernie` profraw and look for shared
   function names with differing hashes / counter counts.
2. If live, the containing fix is to keep the pool single-binary: point spawned
   `lernie` processes at an `LLVM_PROFILE_FILE` outside `target/tarpaulin/profraws`
   at the e2e spawn seam (env is inherited, so one scrub covers the whole subtree).
   MEASURE FIRST: production lines covered only by a spawned binary would then
   need real in-process tests, and the 100% floor is non-negotiable.
3. Alternative posture the parent ball raised: move measurement to cargo-llvm-cov
   (real `llvm-profdata` + `llvm-cov` with every object passed in), as sibling repo
   brazen uses. Bigger change — the denominator is not the same one tarpaulin counts.

Do NOT drop the 100% floor and do not add a blanket exclusion.