+++
title = "Reapply the bl-a227 README rewrite clobbered by bl-cd6b's squash-merge"
created = 1785028264
updated = 1785028264
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
bl-a227 landed on main as 4c712d7 (README compaction + dispatch narrative rewrite, 117+/51-). The very next commit, c135f11 [bl-cd6b], squash-merged from a worktree that branched BEFORE 4c712d7 and its README.md blob reverted every one of those lines: main's README once again says 'runs the terminal compactor off the tip', 'the compactor is a stub', 'worker.rs still stops at the dispatch commit', and carries the failing inspection commands (show --stat 'the compaction merge commit'; show <branch>:summary/001.md).

Fix: reapply 4c712d7's README.md diff on top of current main. bl-cd6b's OWN README change — the stderr.log paragraph appended to the Providers 'Retry is the harness's' bullet — is in a section bl-a227 never touched, so it must survive: reconcile, do not revert c135f11.

Verification already done under bl-a227 (all against a scratch LERNIE_HOME with bz 0.0.3 and a live local ollama model): lernie prime/new/prompt, the four inspection commands, lernie dispatch worker (child ran a full step loop and deposited its result), lernie dispatch compactor (real model call, wrote summary/001.md via write_summary), --goal rejected for compactor, lernie tool read_file/bash, lernie stop/scan/message/bundle/replay, and make smoke SMOKE_PROVIDER=local SMOKE_MODEL=qwen3.5:9b (PASS).

Gate note: the e2e suite execs the real bz, so the close gate needs the pinned bz 0.0.3 on PATH while Cargo.toml reads brazen = '=0.0.3'; the ambient 0.0.4 fails 7 e2e tests on the load-time version guard.