+++
title = "gate: alignment"
created = 1785129905
updated = 1785129926
claimant = "Ferrule"
parent = "bl-c1b9"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Verified post-delivery against docs/ARCHITECTURE.md, docs/PRINCIPLES.md, docs/TAXONOMY.md:

- PRINCIPLES.md 'Single source of truth' (§, 'Copy is the bug... derive don't mirror'): honored. The pin's one home is the `brazen = "=<pin>"` line in Cargo.toml; crate::prompt::brazen_pin() is the one reader, reused as-is by both the pre-existing load-time guard (check_bz_version) and the new cli_version(). No second hard-coded version string was introduced.
- ARCHITECTURE.md §4.4 'Version skew is guarded': updated in this ball to document that lernie --version now also surfaces the pin, keeping the doc coherent with the shipped behavior.
- TAXONOMY.md / ARCHITECTURE.md §2.1 banned-terms list ('call', 'turn', 'session', 'compression' bare usages): no banned bare terms introduced in the code, tests, docs, or commit message added by this ball. 'pin' is not a project term of art requiring a TAXONOMY entry — it is used descriptively (as ARCHITECTURE.md §4.4 already did pre-existing) for the exact-version dependency spec, not a coined term of art.

No misalignment found.