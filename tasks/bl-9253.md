+++
title = "Cut 0.0.1: push main, drive release-plz to a tagged crates.io publish"
created = 1784955403
updated = 1785129878
claimant = "capshead"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
BLOCKED on bl-53e3 AND bl-6f1b. 0.0.1 IS NOT RELEASED. State as of 2026-07-26, Halyard.

=== WHAT LANDED ===
- Stale claim (Gantry, dead session) released; claimed by Halyard.
- PUSHED main twice: e3123ba..39c10a5, then 31cb7d0..0a6a36a. origin/main == local main (0 ahead / 0 behind). Working tree clean.
- CI ON MAIN WENT GREEN at 39c10a5 (run 30216420512, 10m52s, first pass, no flake). The old BLOCKER 1 is dead: cbdc9d4 [bl-636b] + 1588f1c [bl-8366] resolve the brazen pin from Cargo.toml ('make brazen-pin') and pin-cache both bz homes, so the five real-bz e2e tests pass on the runner.
- Old BLOCKER 2 (CARGO_REGISTRY_TOKEN) is dead: f831a89 [bl-6cfb] moved publishing to crates.io trusted publishing over OIDC. 'gh secret list' is empty and that is CORRECT. Trusted publishing is bound to the workflow FILE .github/workflows/release-plz.yml — NEVER rename it.
- PR #1 'chore: release v0.0.1' is MERGED (merge commit 31cb7d0; branch release-plz-2026-07-25T00-17-04Z deleted). Its held bot workflow run 30216460458 was approved with 'gh api --method POST /repos/mudbungie/lernie/actions/runs/30216460458/approve'; CI then went green; the diff was re-verified CHANGELOG-only (+205/-0, new file, version 0.0.1, Cargo.toml untouched) immediately before merging. CHANGELOG.md is now on main. Nothing about PR #1 required an escalation.

=== BLOCKER 3 — 'release-plz release' aborts before publishing. FILED AS bl-53e3 (p9) ===
Release-plz fired correctly on the green CI (workflow_run run 30216800926); the 'release-plz release' job FAILED in 23s at the 'Run release-plz' step:
    ERROR Package `agent-eval` has `publish = false` or `publish = []` in the Cargo.toml, but it has `publish = true` in the release-plz configuration.
Hard abort of the whole release command — lernie itself never got published, tagged, or released.
CAUSE: crates/agent-eval/Cargo.toml has `publish = false` (a936923 [bl-8801]); release-plz.toml's agent-eval block sets only `release = false`. release-plz's per-package `publish` key defaults to true and is validated INDEPENDENTLY of `release`, so the two files disagree.
FIX: add `publish = false` beside `release = false` in release-plz.toml's [[package]] agent-eval block. Config only, one line.
Never caught before because the 'release-plz PR' job succeeds — release-pr skips this check; only `release-plz release` runs it, and it had never got that far while CI was red.

=== BLOCKER 4 — CI on main's HEAD is RED. FILED AS bl-6f1b (p9) ===
After converging (merge of origin/main into local main to pick up PR #1's merge alongside d793c52 [bl-7e33], a parallel agent's close) and pushing 0a6a36a, CI went red and stayed red 3/3 (run 30216944990 plus two 'gh run rerun --failed'):
    test prompt::tool::builtin::bash::tests::cascade_kills_child_when_stop_flips ...
      WARN cargo_tarpaulin::statemachine::linux: Failed to find process for pid: 8432
     ERROR cargo_tarpaulin: Failed to run tests: A segfault occurred while executing tests
No assertion fails — cargo-tarpaulin's ptrace state machine loses the test's spawned child and reports a segfault. This is NOT the known spawn_retries_past_transient_etxtbsy flake.
Bisect data: 39c10a5 GREEN, 31cb7d0 GREEN, 0a6a36a RED 3/3. The only delta into 0a6a36a is d793c52, which is COMMENT-ONLY in tarpaulin.toml (+17/-1, functional body unchanged) — so it cannot be the cause; the red runs were all ~2m on fully warm caches while the green ones were 3-11m on cold caches, pointing at timing sensitivity rather than the commit. See bl-6f1b for the full write-up.
CONSEQUENCE FOR THIS BALL: the release job gates on a green CI run on main via workflow_run. With main red, release-plz cannot fire at all.

=== END STATE — VERIFIED, NOTHING WAS RELEASED ===
- crates.io: lernie max_version 0.0.0; versions ['0.0.0'] only (checked with curl -A 'lernie-release-check (mudbungie@gmail.com)' https://crates.io/api/v1/crates/lernie).
- origin tags: NONE — 'git ls-remote --tags origin' is empty. (Note: the changelog's 'v0.0.0...v0.0.1' compare link points at a tag that does not exist. Cosmetic.)
- GitHub Releases: none — 'gh release list' is empty.
- No binary artifact; the 'lernie x86_64-unknown-linux-gnu' job never ran.

=== REMAINING PATH TO A PUBLISHED 0.0.1 ===
1. Land bl-6f1b (CI green on main again) and bl-53e3 (release-plz.toml publish=false) — both in worktrees, both independent of each other.
2. Push main. Green CI re-fires release-plz via workflow_run.
3. 'release-plz release' then cargo-publishes 0.0.1 over OIDC, tags v0.0.1, cuts the GitHub Release from CHANGELOG.md (already in tree), and attaches the linux-gnu archive.
NOT CLOSING until crates.io shows lernie 0.0.1.