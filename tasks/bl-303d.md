+++
title = "README does not document the opt-in commit-identity guard shipped in tests/commit_hygiene.rs"
created = 1785133394
updated = 1785133394
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
bl-3ac2 landed tests/commit_hygiene.rs on main (c3b4c84) but its README subsection was lost in a close retry (the worktree's uncommitted edit did not survive a 'main moved under the delivery' abort), so the guard ships undocumented.

Add a '### Commit-identity guard (opt-in, per machine)' subsection to README.md under Contributing, immediately before '## License': what main's identity invariant is, that the test arms itself ONLY on $XDG_CONFIG_HOME/lernie/enforce-commit-identity (default ~/.config/lernie/enforce-commit-identity) and is inert everywhere else including public CI, what it asserts over refs/heads/main, that github-actions[bot] stays allowed because release-plz authors the release commit as it, and that the policy lives in the marker — rm it and the guard is off, no code edit.

Docs-only.