# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [0.0.1](https://github.com/mudbungie/lernie/compare/v0.0.0...v0.0.1) - 2026-07-26

### Changes

- experiments/baseline/workflow.yaml duplicates template/workflow.yaml — derive the baseline, don't copy it [bl-aa9a]
- stop cascade pgid discovery can signal the wrong process group — e2e stop tests killed their own gate run under load [bl-5f0c]
- stop --stop-children has no integration proof [bl-3761]
- brazen pin =0.0.3 is stale: 0.0.4 is published and 0.0.3 cannot parse current brazen config [bl-8c92]
- Crates.io packaging: metadata, exclude repo-development files, resolve the eval island [bl-8801]
- e2e tests resolve bz from PATH — parallel agents' installs race; resolve a repo-local pinned adapter for tests [bl-8366]
- template workflow.yaml declares spawn_root_agent — the interpreter has no arm for it [bl-0e79]
- Doc/terminology batch: session POSIX carve-out, README drift, test-double calls renames [bl-81a0]
- drop_oldest_steps overflow policy is inert by construction — subtract or justify [bl-7846]
- lernie stop during a model call: executor exits non-zero, contradicting the §2.9 clean-exit contract [bl-5156]
- Close gate flakes under parallel-agent load: spawn_retries_past_transient_etxtbsy races two wall clocks [bl-7a3f]
- ci.yml never installs bz — five real-bz e2e tests fail on GitHub CI [bl-636b]
- adapter stderr is discarded — startup failures masquerade as killed-mid-stream [bl-cd6b]
- Run tests/install.rs in the close gate (outside tarpaulin) [bl-f01f]
- adapter stderr is discarded — startup failures masquerade as killed-mid-stream [bl-cd6b]
- README compaction + dispatch narrative describes deleted code [bl-a227]
- compaction merge imports the compactor's private transcript into the parent [bl-4fdf]
- read_file oversize reports fabricated size; lernie new on an unseeded data root drops descriptions/ silently [bl-71b8]
- lernie config: declined/failed authoring pass leaks the transient checkout and wedges the verb [bl-54aa]
- replayed workspace cannot be driven — no config/* ref survives [bl-aabb]
- Author docs/USER_STORIES.md: the promise suite lernie 0.0.1 is evaluated against [bl-0135]
- summarize is inert at assembly — wire it to trigger the §6 compaction checkpoint [bl-a1a1]
- Switch crates.io publishing to trusted publishing (OIDC), drop CARGO_REGISTRY_TOKEN [bl-6cfb]
- strengthen command_surface_parity.rs to argument- and product-level bijection [bl-4762]
- Validate agent id at the inbox boundary; lernie message errors on a nonexistent agent [bl-bdc7]
- Description-always is partial: a standalone skill's frontmatter composes nowhere [bl-ae66]
- bash/tool subprocesses run in the operator's cwd, not the agent's worktree [bl-2503]
- child terminal deposit does not launch the parent's driver — parent revival needs a manual scan [bl-4a6c]
- Coverage measurement flake: intermittent 1-line miss on unrelated diffs (llvm region attribution) [bl-1c2e]
- inject the adapter target through Fx; publish to crates.io [bl-612a]
- Stale brazen pin: Cargo.toml pins =0.0.2 but brazen 0.0.3 is published [bl-143e]
- release-plz.yml header comment says 'Three jobs' but there are four: describe prune-release-branches [bl-6b52]
- Prune superseded release-plz-* branches in release-plz.yml [bl-2e35]
- Add build-gated release-plz release automation [bl-2e8d]
- live-wire smoke test: the first real model call (make smoke) [bl-06d5]
- child_result.rs is 302 lines — over the 300-line code cap on main [bl-19cb]
- :Terminal(Epitaph) and AdvanceHandoff::Done payloads are carried but read only by tests [bl-4f6d]
- Manifest has no runtime caller because §5.2 context assembly is unimplemented: the assembler ignores manifest.yaml [bl-e0cb]
- lernie new: seed control files from config-root template/ override (embedded fallback) [bl-e795]
- Unify the §3.3 tool-resolver third hop with the injected driver target (linked-host correctness) [bl-eb98]
- Config machinery has no runtime caller: wire manifest/version/loaders/schema-gen into verbs or subtract them [bl-9e2d]
- Update README GUI pointer: sibling repo renamed lernie-ui-egui -> yog [bl-60f7]
- Library port: expose the command surface as a lib (exact-pin consumers) + parametrize the driver/successor exec target [bl-231c]
- lernie needs a bootstrap/init verb: idempotently found the installation substrate (config root + data root) honoring LERNIE_HOME [bl-6d83]
- Open the dispatch role set: lernie dispatch <role> accepts any config-defined role [bl-f72b]
- the nature of a role — what it is, what it binds, where it lives [bl-3a85]
- workspace substrate — config branches, agents/* refs, no main (§2.2–§2.3 physical) [bl-a51c]
- Invalid model id claude-sonnet-4-7 shipped as worker default; no live call ever caught it [bl-3157]
- main fails make check: repo-wide rustfmt drift; pin the toolchain [bl-f830]
- evaluation layer — task suite, agent-eval, bundle/replay (§9, v0.9–v0.10) [bl-8094]
- Decouple the GUI: extract lernie-ui-egui to its own repo, strip crate + tethers from lernie [bl-bdd2]
- derive BRAZEN_PIN from Cargo.toml (single home for the brazen pin) [bl-51ef]
- Root Cargo.toml version 0.1.0 -> 0.0.1 for first crates.io release [bl-58a8]
- crates.io publish hygiene: publish=false on lernie-ui-egui, repository field on root [bl-a908]
- workflow actions v0.7: remaining executors + verifier-gate end-to-end [follows bl-6a3b] [bl-bd8a]
- wire compaction bindings live: dispatch(compactor) on checkpoint + compaction_merge on return, role-aware advance [bl-2d5a]
- agent-eval --config/--suite/--runs (v0.10) [bl-25fc]
- real compaction — model-driven compactor, checkpoint triggers, the compaction merge (§2.6–§2.7) [bl-9dbd]
- workflow actions — event→action bindings act at runtime (§6, v0.7) [bl-6a3b]
- alignment [bl-4af9] [bl-09d4]
- child step loop — dispatched children run to terminal and deposit (§2.5 live) [bl-c33b]
- skill body-on-demand — agent-elected skill load into the worktree (§3.3) [bl-4af9]
- talk up the exec baton — image reload as structural state-safety (§6, §3.1, PRINCIPLES) [bl-0855]
- config-commit authoring verb: harness-assisted config edits beyond lernie new [bl-2774]
- §7.1 workspace view + §3.5 agent-state classification [bl-46b4]
- docs [bl-a51c] [bl-f48f]
- workspace substrate — config branches, agents/* refs, no main (§2.2–§2.3 physical) [bl-a51c]
- evaluation layer — task suite, agent-eval, bundle/replay (§9, v0.9–v0.10) [bl-8094]
- lernie advance — the driver verb (exec baton, lease handoff, launch detachment) [bl-4684]
- stop during tool execution: wire SIGTERM to run_tool_calls stop_flag for the clean stopped-deposit exit [bl-f2a8]
- alignment [bl-5846] [bl-b0da]
- docs [bl-5846] [bl-a69e]
- scan runs at step frequency for crash-rate events — segment driver/reviver/janitor [bl-5846]
- Agent-level stop cascade: --stop-children walks the id namespace; kernel pgid scoped to one executor [bl-535d]
- lernie stop: discover executor pid via lock fd, not response.json [bl-aafc]
- re-voice §3 process model (threads/daemon voice) + NFS unsupported-config note [bl-8122]
- coherent vs ARCHITECTURE/PRINCIPLES/TAXONOMY [bl-89ed]
- Stop handler: SIGTERM deposits the stopped epitaph [substrate] [bl-9f53]
- reflect current state [bl-cc15]
- Startup scan: silent-death sweep deposits and inbox flush [substrate] [bl-d148]
- Delete merge/: result-message return replaces rebase-then-merge-back [substrate] [bl-4ce8]
- Rename assistant origin token: model id authors the entry [substrate] [bl-79aa]
- Delivery drain: step-boundary inbox drain and delivery commits [substrate] [bl-1129]
- Re-point context assembler at the transcript; delete accumulator and output.json read path [substrate] [bl-26cb]
- Inbox substrate: executor flock, deposit, lernie message verb, message tool [substrate] [bl-cb44]
- Transcript writer: stream assistant output to staged transcript entries [substrate] [bl-4798]
- §2.11 inbox substrate — deposit, delivery, executor lock [substrate] [bl-4298]
- §2.3 transcript writer — context has one home [substrate] [bl-a847]
- Delete await_tool: await/check have no referent once step 5 is total [substrate] [bl-2ca1]
- dissolve await — every terminal event deposits a result message [substrate] [bl-65d8]
- Wire descriptions-always producer: populate descriptions/{tools,skills}/ at conversation-repo creation [bl-3092]
- workspace substrate — one repo, config branches, agents as worktrees; merge-back eliminated [bl-b09c]
- context has one home — committed transcript, no in-RAM history, append-only assembly [bl-c904]
- XDG harness-root split: config/data dirs, LERNIE_HOME collapses both [bl-c6e7]
- terminology ladder v2 — workspace / config / agent; exchange demoted to span [bl-08df]
- Collapse §6 budgets to one live whole-tree check; delete clamped inheritance [bl-f48c]
- docs gate: descriptions-always producer bl-3092 (§3.3 amendment) [bl-d7a3]
- message delivery vs blocking await — reminder-shaped children pair with check(), or await grows a timeout (§2.5/§2.11) [bl-2ff2]
- Compose role tools into the model call: load descriptions/tools/*.json into CanonicalRequest.tools [bl-9e96]
- adversarial sender defense — record messaging as a deferred lateral prompt-injection surface (§11) [bl-962d]
- Per-conversation budgets (v0.7, ARCH §6). workflow.yaml budgets:{max_total_tokens,max_wall_seconds,max_depth} (config/workflow.rs Budgets; schema regen). New prompt::budget: derive.rs (spend sums Usage across every attempt segment; wall sums per-step started_at->ended_at spans incl backoff; depth from hyphenated descent; all over branch+descent, derived from disk, no counter=SSOT; None counter=0) + mod.rs (check at boundary: tokens/wall exhaust at >=limit, depth positional at >max_depth, unbounded never fires; remaining()+clamp() = min(parent_remaining,child_declared) per axis; mark_exhausted writes refs/lernie/budget-exhausted/<branch>, the only new artifact, git-ref like §2.6 conflicted). Enforcement in dispatch::run_exchange before model_call::run; exhaustion writes ref + ceases loop (no compaction/merge). await gains {status:budget_exhausted}. Surgical §6 amendment documents derivation scope + max_depth boundary (deepest allowed; root=0). Folded latest main (bl-3eea) in first. 26 new tests; 100% workspace coverage; all files <=300 lines. DEFERRED (filed separately recommended): child step-loop doesn't exist yet, so clamp/remaining are unit-tested + the dispatch-time hand-off wires when subagent loops ship. [bl-9e9b]
- §2.11 writer/driver totality amendment delivered [bl-3eea]
- Fix 1: scrubbed INHERITED_GIT_ENV (GIT_DIR, GIT_WORK_TREE, GIT_INDEX_FILE, GIT_OBJECT_DIRECTORY, GIT_PREFIX, GIT_COMMON_DIR, GIT_ALTERNATE_OBJECT_DIRECTORIES) from tests/prompt_retry.rs's branch --list --no-merged git subprocess, mirroring lernie-ui-egui::git_tree::cmd::INHERITED_GIT_ENV. Reproduced: GIT_DIR=$(pwd)/.git cargo test --test prompt_retry retryable_529 failed before, passes after. prompt_end_to_end.rs already had the scrub. Fix 2: corrected 3 stale <harness-root>/providers.yaml refs in src/config/per_repo_providers.rs to models.yaml (module doc, load() doc, LoadError::Invalid message); per-repo providers.yaml left untouched. cargo test --workspace: 373+ passed 0 failed across all crates. tarpaulin --workspace --fail-under 100: 100.00% (2562/2562). [bl-4a9b]
- v0.6 alignment gate: shipped brazen provider layer audited COHERENT against ARCHITECTURE/PRINCIPLES/TAXONOMY. Verified at main 30e68f2 inside work/bl-a46f. All four axes PASS; one surgical doc-coherence edit made; no code touched. [bl-a46f]
- v0.6 brazen docs gate. README/template/schemas match shipped state (no lernie-provider refs; make schemas produces no diff — committed schemas current; models.yaml/providers.yaml split correct). Fixed README build-targets make-install line (was dropping retired lernie-provider-anthropic into adapters/; now documents cargo install brazen --version =0.0.2 + models.yaml/agents/default layout). ARCH §4.4 spot-checked against src/prompt/dispatch/model_call.rs, adapter.rs, config/models.rs, per_repo_providers.rs — all claims hold. Folded the three bl-56ee divergences into §4.4/§12 (config-only backoff/no retry_after_seconds; unset Options serialize as null in request.json; compactor still v0.3 stub). Also fixed a non-historical shipped-state divergence at ARCH §2.2 (frozen-copy bootstrap claimed the harness resolves auth/endpoint against a global providers.yaml — corrected to brazen config+credstore, global models.yaml). Reported: pre-existing test env bug in tests/prompt_retry.rs (GIT_DIR leak, owned by bl-d653) and stale global-providers.yaml doc strings in src/config/per_repo_providers.rs — neither fixed per scope. [bl-660b]
- Data-plane swap to brazen bz: exec per attempt, harness-owned retry, config fold, delete lernie-provider-anthropic (ARCH v0.4 §4.4, §2.10, §3.5, §4.1-4.3). [bl-56ee]
- front-door messaging (§2.11) — queue model, delivered [bl-ed40]
- Additive brazen dual-vocabulary read-side transition. Linked brazen =0.0.2 for canonical TYPES only (use brazen::Event; matched on Event::End / Event::Error(_) — NO runtime call, verified by grep). New src/provider/segment.rs (+segment/tests.rs): classify(bytes)->Outcome{Complete,Failed,NoTerminal} over a CLOSED response.json per §4.4 reading rules (last segment authoritative; refusal is a Finish=>complete; multi-segment failed-then-clean=>complete). The dual-vocab seam is isolated in segment.rs::legacy_terminal() (message_stop) — the single piece bl-56ee deletes; brazen  recognized via the linked Event type. Taught readers: await_tool/state.rs (routes framing through segment::classify, dropped local Examined/classify_last_line), UI git_tree/state.rs::has_terminal_event (+brazen end), UI git_tree/streaming.rs::accumulate_text_deltas (+brazen content_delta). brazen fixtures added alongside legacy in segment tests, git_tree/state, git_tree/streaming, await stopped tests. Legacy anthropic data plane untouched. cargo test --workspace green; tarpaulin --workspace 100% (3023/3023). SPEC GAP flagged for bl-56ee/docs gate: the 'terminal classification only after fd close' rule (bl-5811 §3.5/§4.4) is not yet enforced — await Complete/Failed and UI terminal=>Stopped classify on terminal-presence without the IN_CLOSE_WRITE/fd-open guard; correct for today's single-attempt writer, but must gate on fd-close once bl-56ee ships the fd-held-open multi-attempt retry loop. [bl-507a]
- Design deliverable: ARCHITECTURE §3.6 'Sandboxed tools (v1.1)' — the full spec the §12 v1.1 milestone bullet summarizes. [bl-0bae]
- Resolved six attempt-segment spec holes in docs/ARCHITECTURE.md (design task; no code touched). Edits surgical, matching existing voice/cross-ref style; no banned terms introduced; SSOT preserved (segment count and spend stay derived, never stored). [bl-5811]
- ARCHITECTURE Draft v0.4: harness-repo fold — brazen is the provider layer. §4 rewritten around the bz pipe contract (canonical request stdin -> v=1 event NDJSON stdout, one process per attempt, exactly one HTTP round-trip); vocabulary linked from the pinned brazen crate (types only — data plane stays subprocess per §3.4; retryable() gets one home; version-skew guard at load, MessageStart.v handshake under adapter: override); retry moves harness-side (brazen non-goal) with attempt segments appended to response.json, terminal event message_stop -> end everywhere (§2.3/§2.9/§3.5), api_calls dissolved into derived segment count (§2.1/§8); auth/endpoints fully delegated to brazen config+credstore (auth_env/endpoint_env retired); global providers.yaml -> models.yaml (capabilities/context_window only + adapter: escape hatch); retry knobs + budgets(v0.7: max_total_tokens/max_wall_seconds/max_depth, clamped child inheritance, refs/lernie/budget-exhausted + await status) declared in workflow.yaml §6; §12 inserts v0.6 brazen milestone, renumbers workflow->v0.7/tasks->v0.8/experiments->v0.9, adds v1.1 sandboxed tools (WASM/WASI manifest-intersect-grant over the unchanged §3.3 stdio contract). TAXONOMY provider-adapter entry + PRINCIPLES integrations paragraph updated. Rejected from the harness repo: transcript-as-JSONL SSOT (git substrate is richer), linked-generate() data plane (CLI-as-control-plane wins). docs/LHCS.md + docs/WDF.md (untracked May drafts) are superseded by this spec and recommended for removal. [bl-f739]
- Add `make ui REPO=<path>` target [bl-4d80]
- alignment gate: statelessness audited (no UI write paths to repo state, subprocess surface is read-only git CLI + lernie-subcommand exec only), no banned terms, pluggability claim demonstrated by tests/pluggability.rs (4 parallel GitTree::from_repo derivations against one fixture observe identical view-models). Caveat: the smoke test's first iteration uncovered a fork/exec env-inheritance hazard (GIT_DIR leaks from cargo-test into the git subprocess and silently redirects fixture commits onto the outer repo) that production code already mitigates in git_tree::cmd::INHERITED_GIT_ENV; the test now applies the same scrub. Worth a v0.6 ARCH §3.5 mention if any future pluggability checklist gets written, since the same trap will catch every future second-frontend's test fixtures. Also includes the bl-c9ec rustfmt straggler (await_tool tests) so make ci stays green on the merged state. [bl-a008]
- detect kill-mid-stream subagents via /proc fd scan [bl-c9ec]
- docs gate: README §UI updated to drop the v0.5-skeleton framing and replace the 'live updates wire through here in a follow-up' placeholder with the shipped tick model. ARCH §3.5 + §7 already match shipped (pluggability contract, alternate-frontend pointer, branch-state classification, streaming watch path, terminal-event derivation) — verified, no spec edits needed. The egui/eframe choice + how-to-run + alternate-frontend (lernie-ui-web) contract are all explicit in the now-current README. [bl-686e]
- tests gate: cargo test --workspace + tarpaulin --fail-under 100 green on merged state. 645 tests pass, 2905/2905 lines covered. Two findings, both fixed in this gate's commit: (1) ETXTBSY flake on the write_script fixture sanity check — wrapped exec in the same retry budget production code uses; (2) P11 landed with non-fmt'd UI files under rustfmt 1.8 — re-fmt'd. End-to-end demo per epic done-when validated component-wise: the harness side via tests/prompt_end_to_end.rs (httpmock-driven full lernie new + lernie prompt cycle, asserts §2.6 merge=ours topology); the UI side via component tests (fs_watcher, cli_outbound, git_tree, streaming, actions); the FS contract between them is the only integration surface. Live visual demo (real Anthropic call + windowed UI) is the user's verification bar — not an agent-runnable gate. [bl-5fca]
- v0.5 P11: UI new-prompt + stop buttons via cli_outbound [bl-163d]
- v0.5 P10 (bl-a144): lernie stop <repo> <branch> — cascading SIGTERM (ARCH §2.9). Pid via /proc/<pid>/fd/* writer scan against latest response.json (no sidecar pid file — same source the §3.5 in_flight classification reads). lernie prompt setpgid(0,0)s itself so the kernel cascade reaches its provider adapter and any subagent harnesses re-entered via lernie dispatch. Idempotent for already-stopped branches; errors for missing or already-merged ones. Linux-only. [bl-a144]
- v0.4 P3: await built-in resolves dispatch handles [bl-829f]
- ARCH/PRINCIPLES amendment: excise user-action resume; lernie advance [bl-abf3]
- v0.4 P2: dispatch built-in tool spawns subagent via §3.4 CLI [bl-175a]
- v0.4 Phase 1: generalize lernie dispatch CLI for arbitrary roles [bl-88f4]
- v0.3 docs gate: README step renumber + verification [bl-60aa]
- v0.5 UI: branch-state indicators (in_flight / stopped, derived) — amends ARCH §2.9 to drop the cancel marker, classifies via §4.4 terminal events, badges in the git tree [bl-de6b]
- v0.3.1 alignment: §2.2 per-tool runtime-read characterization tightened [bl-f08f]
- v0.3.1 docs gate: drop transitional note + scrub stale steps paths [bl-0c2e]
- pin tarpaulin 0.35.2 to stop silent denominator shrink [bl-ae88]
- bump actions/checkout to v6, actions/cache to v5 [bl-3db1]
- v0.3.1 P3: response.json as JSONL stream + writer-closes-fd completion [bl-584d]
- configure git identity on the runner [bl-8f56]
- add llvm-tools-preview component for tarpaulin [bl-45e7]
- v0.3.1 P2: harness step record relocation + snapshot simplification [bl-10dd]
- v0.3.1 P1: ARCH amendment for step records out of worktree [bl-4117]
- Add GitHub Actions CI workflow running make ci [bl-e99b]
- split user quickstart from contributor setup [bl-b8ec]
- Add gitignore
- v0.3 built-in tool: bash [bl-ecf1]
- Phase 7: make install harness root scaffold [bl-66da]
- v0.3 built-in tool: read_file [bl-a96a]
- v0.3 multi-step exchange loop [bl-9221]
- v0.5 UI: v0.3-shape fs_watcher prefixes + git_tree renderer [bl-3465]
- v0.3 tool executor: SpawnTool + per-call disk record [bl-7c2a]
- Phase 6: schema regen + final string sweep [bl-d135] [bl-d135]
- Phase 5: compactor at summary/<NNN>.md + merge=ours discipline [bl-d944]
- v0.3 wire types: tool_use / tool_result blocks + tools request field [bl-8be5]
- Phase 4: soul/role machinery + agents.yaml retirement [bl-70b9] [bl-70b9]
- Pin v0.3 tool contract in ARCH §3.2/§3.3/§4.3/§11/§12 [bl-fdb9] [bl-fdb9]
- Phase 3: v0.3 prompt path migration [bl-7bca]
- tests gate green: fmt + clippy + 100% coverage on streaming epic [bl-4033]
- Adapter streaming wire (§4.4) [bl-de80]
- Phase 2: v0.3 conv-repo template + scaffold [bl-ecda]
- Phase 1: harness root + global/per-repo providers split [bl-d7b1]
- Redesign conversation-repo layout; retire invocation as structural term [bl-32ab]
- ARCH + PRINCIPLES: add Regenerability, name the workflow interpreter [bl-cb63]
- Harness neutral adapter-wire types in src/provider/wire.rs [bl-bf79]
- Extract lernie-provider-anthropic to its own workspace crate [bl-f8bd]
- Add --request <path> file-input flag to lernie-provider-anthropic (additive per ARCH §4.4); invocation semantics identical to stdin. Docs: one-sentence §4.4 addition. Tests: 2 new subprocess integration tests; fixed pre-existing coverage flake in cli_outbound drop_escalates_to_sigkill_if_sigterm_ignored (readiness-marker sync avoids SIGTERM-before-trap race). make check 100% stable across 3 runs. [bl-becc]
- alignment gate: compactor as CLI dispatch + dispatch goal.md per §3.4 + §2.8 [bl-5acb]
- §4.4 non-streaming response shape pinned to Anthropic wire pass-through; parser tests pin required fields [bl-2c54]
- git-tree renderer for v0.2-shape repos [bl-8af1]
- v0.2 docs gate: reflect shipped shape, retire v0.1 callouts [bl-535f]
- v0.2 tests gate: verify end-to-end green, fix two flakes [bl-4a6b]
- terminal compaction stub + no-ff merge back [bl-8c66]
- git-tree renderer for v0.1-shape repos [bl-209e]
- scrub branches.json nonexistence checks [bl-574a]
- CLI outbound module (exec lernie subcommand) [bl-02da]
- filesystem watcher module (inotify + polling fallback) [bl-ea1d]
- retire branches.json; single-source-of-truth principle [bl-4652]
- lernie-ui-egui skeleton (workspace + placeholder binary) [bl-1013]
- track unmerged branches in branches.json [bl-a764]
- spawn exchange branch + step commits [bl-e7e1]
- harness/UI roles + filesystem-as-event-stream contract [bl-bcd9]
- retire --endpoint argv; describe-driven endpoint discovery [bl-effb]
- install target copies binaries to ~/.local/bin [bl-b64e]
- lernie prompt subcommand — commit one exchange per invocation [bl-e048]
- 'Everyone uses the front door' principle + §3.4 CLI as control plane [bl-dfb7]
- extract Anthropic adapter as lernie-provider-anthropic binary [bl-7cfa]
- provider adapter contract — externalize provider layer [bl-415c]
- disk-first rewrite and CLAUDE.md -> AGENTS.md symlink [bl-621d]
- Anthropic Messages API client (blocking, non-streaming) [bl-9600]
- lernie binary with 'new' subcommand for conversation scaffolding [bl-2904]
- scaffold conversation repo under template/ [bl-c91c]
- promote 'Disk first' to lead principle, fold in the bus framing [bl-4d73]
- alignment gate covers ARCHITECTURE.md and PRINCIPLES.md, not just TAXONOMY.md [bl-90a9]
- schema fields, enum values, examples, action DSL, and event names all conform to ARCH §2.1 / TAXONOMY.md (uses 'model call', 'tool call', 'API call' explicitly via Action variants; 'exchange', 'invocation', 'branch', 'goal', 'dispatch' as defined; 'compaction' for the harness operation). Banned-term grep is clean over src/, tests/, schemas/. Removed an English 'in turn' idiom from a comment for hygiene. [bl-adf6]
- load+validate manifest, workflow, providers, agents, version [bl-5bbc]
- MIT LICENSE + Cargo.toml license field + README note [bl-ffb8]
- Tighten arch spec: streaming, compaction, hooks, retry + principles [bl-2736]
- revise branch topology — dispatches branch, steps commit [bl-32fd]
- align ARCHITECTURE.md to TAXONOMY.md terms [bl-cd2b]
- rust crate with makefile and precommit hook [bl-fe61]
- initialize
