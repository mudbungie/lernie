+++
title = "gate: tests"
created = 1785125509
updated = 1785129526
claimant = "Oakum"
parent = "bl-1c94"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"

[[blockers]]
id = "bl-1c94"
on = "claim"
+++
Gate: tests for bl-1c94 (epitaph-gated `compaction_merge`) — **PASS**.

- CI on the landing commit **6d4412b** is green: GitHub Actions run
  30220130316 (`CI`, push, main), success. `make check` there is
  fmt-check + clippy `-D warnings` + `cargo tarpaulin --fail-under 100`
  + the install contract, so 100% coverage holds on the landed tree.
- Re-run locally by Oakum in a worktree at 6d4412b:
  `cargo test --workspace child_result` — **23 passed, 0 failed**,
  including the two tests this ball's fix landed:
  `a_died_compactor_return_lands_no_merge_and_delivers_the_epitaph`
  and `a_stopped_compactor_return_lands_no_merge_under_an_explicit_binding`,
  alongside the unchanged
  `a_compactor_result_lands_the_compaction_merge_and_consumes_the_message`
  (the `final-response` path still merges).
- Parent revival is the flush/probe seam and is epitaph-agnostic; it
  stays pinned by the scan sweep tests (all green, see bl-cdea).
