+++
title = "main fails make check: repo-wide rustfmt drift; pin the toolchain"
created = 1784336702
updated = 1784337252
claimant = "Sorehead-f830"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
PREMISE CORRECTED (living-document): the ball said main fails cargo fmt --check with 62 hunks. Verified this session at main 71ee3b2: fmt-check is CLEAN and clippy is CLEAN under installed stable (rustc 1.95.0 / rustfmt 1.9.0). The 62-hunk drift was resolved by the lernie-ui-egui crate extraction (bl-bdd2, landed today) — no repo reformat was needed. Enduring substance delivered:

(1) rust-toolchain.toml — the single home of the Rust pin: channel "1.95.0", profile minimal, components [rustfmt, clippy, llvm-tools-preview]. Exact pin (not floating stable), same lockstep reasoning as the tarpaulin 0.35.2 pin in tarpaulin.toml — a floating channel lets a rustfmt/clippy bump land drift invisibly. Satisfies brazen's rust-version 1.85 (brazen 0.0.2 is actually edition 2021, not 2024 as the ball implied; the binding constraint is rust-version 1.85) and the workspace's edition 2024.

(2) .githooks/pre-commit now invokes 'make check' (fmt-check + lint + coverage) instead of duplicating the tarpaulin command. The Makefile stays the single source of what the gate is; fmt-check and clippy are folded into every bl close. 300-line cap + mainline guard unchanged.

(3) CI: replaced dtolnay/rust-toolchain@stable (floating; it does NOT read rust-toolchain.toml and @master would require naming a toolchain, forking the pin into two homes) with a rustup override trigger ('cargo --version' + 'rustup show active-toolchain'). rustup ships on ubuntu-latest and auto-installs the exact toolchain+components from rust-toolchain.toml on first cargo command, so the file governs CI, hook, and agents identically. No version string duplicated in the workflow.

(4) README updated: CI line, Contributor setup, and the pre-commit-hook section (rule 3 now describes 'make check').

Verified: 'make check' green under pinned 1.95.0 — 100% coverage 3973/3973, fmt + clippy clean.