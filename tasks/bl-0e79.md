+++
title = "template workflow.yaml declares spawn_root_agent — the interpreter has no arm for it"
created = 1784956419
updated = 1784956419
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Emberly G8. The shipped template/workflow.yaml declares spawn_root_agent but the workflow interpreter has no arm handling it — dead vocabulary in the shipped default config. Decide per minimalism: subtract it from the template (and schema if enumerated) OR implement the arm if ARCH specifies it — read ARCH's workflow section first; fix whichever side is wrong. Tests: template round-trips through the interpreter with every declared verb reachable (a totality test so the next dead verb cannot ship).