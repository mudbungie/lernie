+++
title = "the conversation's spine reaches no gesture: the rail, the governing commit, and the fork composed off them"
created = 1788399123
updated = 1788399123
priority = 2
root_commit = "3efc0d263898c425a0ff2bb042938233e838f436"
+++
Three of bl-2cf7's exemption rows repoint here — the seam is the SPINE: what the conversation's history is anchored to, and the one act composed off it.

- `rail` answers the spine's rows (operable commits: seq, commit, budget, cut) and its candidate cards. No decoder in src/reply, fixture in corpus/unreadable/rail.json.
- `governing` answers the config commit governing the conversation — oid, the files it holds, `follows`/`diverged_lineages` (PROTOCOL 5 moved `oid`'s meaning: it names the followed lineage's head now, not the fork commit — read the PROTOCOL note before painting it).
- `fork` is an act, not a read, and lands here because its `from` is a commit an operator can only discover off the rail. Its `role` is a name off the config lineages — the `lineages` read is bl-5c53's subject, so the role half may arrive as a plain text box until that pane exists; say so at the control if it does.

The pattern is bl-2cf7's own: typed decoder in src/reply, fixture moved corpus/unreadable/ -> corpus/answers/, painted inside the records pane bl-2cf7 founded (a covering pane over the conversation, standing read keyed on the pane per Standing's tuning precedent), control tagged act:<op>, exemption line deleted.