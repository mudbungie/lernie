+++
title = "engine rename: this crate becomes litany — census, LERNIE_HOME ruling, mechanical rename"
created = 1787977319
updated = 1787977446
claimant = "OrderRenamer"
priority = 2
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Per the downstream operator ruling adopting a four-component split, this crate — the agent-loop engine — takes the name `litany`. The crates.io name is already held at 0.0.0 by the owner. The `lernie` name does not die: it passes to the seat crate (window + seat client) at a version fence — the engine's line under that name ends at 0.0.x, the seat's begins at 0.1.0, and both READMEs must state the fence, because the published record carries two eras of one name and the fence is the only disambiguation rule.

Scope of this ball: PREPARE the rename. Produce the surface census, resolve the one priced decision in a tracked design note, and land the mechanical rename in the claim worktree.

The priced decision is `LERNIE_HOME` and the state-home layout. Two arms, both real:
- keep the variable name, and the env var diverges from the crate name permanently (two representations of one fact, which drift);
- rename it, and every existing world is a state migration — including the downstream server's nested world, which overrides the variable for every child it spawns.
Resolve explicitly in the design note; do not drift into either by default.

Doc cascade: this repo's ARCHITECTURE / TAXONOMY / tool-injection design docs are cited BY NAME from the downstream server's design docs. The citations are the downstream's to update; this ball records which titles moved.

Publishing is an operator adjudication and is NOT part of this ball: no `cargo publish`, no repository rename, no landing on `main`. The rename landing would trip the release pipeline, and the version fence has to be adjudicated before it does.

---

State: work COMPLETE and COMMITTED on the claim worktree; ball deliberately LEFT CLAIMED and NOT closed.

Why not closed: landing this on main trips the release pipeline, and publishing under the new name is an operator adjudication that has not been made. Two commits sit on the claim branch:

1. docs/DESIGN_ENGINE_RENAME.md — the deliverable design note: the surface census (1990 occurrences over 267 files, grouped by what a rename actually costs), the priced LERNIE_HOME decision with both arms, the rejected accept-both middle, the recommendation and its reasoning, the migration recipe (specified, not run), and the ordered operator acts.

2. The mechanical rename itself. Crate, binary, workspace member, CI and release config, docs, schemas, skills, tests. CHANGELOG.md is deliberately NOT swept — it is the published record of releases made under the old name, and rewriting it would erase the fence the ruling turns on; the fence lands as a new entry there and as a note at the head of the README.

RECOMMENDATION on the priced decision: rename the variable. The two arms are not "clean name versus cheap migration", because the old name is being reassigned to a live sibling component rather than retired — keeping it would point the engine's state root at that sibling's name, and the failure that buys is an operator relocating the wrong component's state with no error. Against that, the migration population is enumerable and the line is at 0.0.x, which is the pre-stability window a break like this exists for. All three durable-state layers move together (the variable, the XDG harness roots, the in-workspace mark ref namespace), because moving one without the others only relocates the divergence. No compatibility shim: a fallback read would be two representations of one fact inside the one resolver every verb calls.

One compatibility break needs an explicit release-note line rather than a silent sweep: the two conversation-context variables handed to tool subprocesses are a published contract to operator-authored tool scripts under the harness root. They are renamed, and a script reading the old names will read an unset variable.

Gate: `make check` run green on this exact tree — fmt-check, lint (leak-scan + clippy -D warnings), coverage 100% at 7974/7974 lines, install contract. Full suite 1462 unit tests plus the integration and e2e binaries, no failures. Two tracked symlinks were clobbered by the sweep and restored; that is the one trap in repeating this mechanically.

Remaining, all operator adjudications: adjudicate the version fence and the final release under the old name; publish under the new name; rename the repository and update the trusted-publisher registration; re-pin downstream and sweep its citations; run the migration on each install.
