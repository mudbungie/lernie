+++
title = "Compose role tools into the model call: load descriptions/tools/*.json into CanonicalRequest.tools"
created = 1783488553
updated = 1783558839
claimant = "Without-9e96"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Flagged during bl-56ee (v0.6 data-plane swap, main de48a2f). §4.3 defines a role's tools: list and per_repo_providers RoleAssignment now parses it, but the harness never loads the tool JSON schemas (from <worktree>/descriptions/tools/*.json per §3.3/§5) into brazen::CanonicalRequest.tools — so the model is never TOLD which tools it may call. Pre-existing gap (the retired lernie-provider-anthropic wire request also omitted the tools array); the multi-step loop still executes whatever tool_use blocks arrive, but the model is flying blind about its toolset. Deliverable: assembler composes the role's declared tools (intersect role.tools with available descriptions/tools/*.json) into the typed CanonicalRequest.tools (brazen::Tool). Tests: a role with tools:[bash] produces a request whose tools array carries the bash schema; empty/omitted -> empty array. 100% coverage, <=300-line files. Confirm brazen::Tool shape (name/description/input_schema) against the =0.0.2 pin.