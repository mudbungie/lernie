+++
title = "fork-from-history and config-branch selection reach the CLI: --from <ref> on prompt/dispatch, --config <name> on prompt"
created = 1785647903
updated = 1785649263
claimant = "Halyard"
priority = 2
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Ruled by yog docs/VISION.md (bl-e722 + bl-3d69) §4.4/§6, operator-confirmed 2026-08-02. ARCH already specs this: §2.3 'Any ref is a legal fork point' (fork-back-in, parallel fan) and §7.2 'Forking from history … is the ordinary fork with a historical ref argument … no distinct operation.' The plumbing exists — fork_point rides SpawnRequest and ChildDispatchRequest — but the only production caller is the verifier (src/prompt/dispatch/child_result/verifier.rs), and no CLI verb takes a ref: prompt is (repo, message), dispatch is (role, repo, branch, --goal). Likewise config branches are creatable (lernie config --from/--orphan) but unstartable: prompt hardcodes DEFAULT_CONFIG_REF (src/workspace.rs). Ask, exactly two arguments and no new verbs: (1) --from <ref> on prompt and dispatch — the ordinary fork with a ref argument, per §7.2; (2) --config <name> on prompt so config/<name> lineages are startable. Downstream consumers: yog VISION rungs V2 (fork-from-any-notch, xN fan) and V3 (judge/consensus — which need NOTHING else: a judge is a verifier-shaped dispatch over sibling terminal refs; no fan-in primitive, refused per VISION §6). Also unblocks lernie's own documented recovery path for the one non-replayable state (ARCH: 'the operator's recovery is the ordinary fork-from-history') which currently has no verb. USER_STORIES gap-table candidates G-fork if that table is updated.