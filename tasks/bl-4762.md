+++
title = "Parity: strengthen command_surface_parity.rs to argument- and product-level bijection"
created = 1784955403
updated = 1784955403
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
User-requested release gate: test-enforced PERFECT parity between the CLI interface and the crate public interface. tests/command_surface_parity.rs already asserts a (kind,name) bijection between pub items of lernie::cmd and clap's introspected verb set. Audit its actual strength: does it enforce parity of each verb's ARGUMENTS (clap args <-> the pub run-entry signatures / Cli struct fields) and PRODUCTS (stdout contract / Outcome types), both directions? Does it cover the prelude re-exports and reject new pub leakage anywhere (not just top level)? Close the gaps by strengthening the test, not by convention. If a gap is structural (e.g. products aren't introspectable), make them introspectable rather than exempting them.