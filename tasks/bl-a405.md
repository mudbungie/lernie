+++
title = "gate: tests"
created = 1785129905
updated = 1785129908
claimant = "Ferrule"
parent = "bl-c1b9"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Verified post-delivery: src/cmd/tests/surface.rs::cli_version_pairs_lernie_and_the_brazen_pin asserts (1) cli_version() starts with CARGO_PKG_VERSION, (2) the parsed `(brazen ...)` component equals crate::prompt::brazen_pin() (the load-time guard's own expected pin — bijection, not duplication), and (3) it is actually wired into Cli::command().get_version(). Full `make check` on this ball's close ran green: fmt-check, clippy -D warnings, 100.00% coverage (5007/5007 lines), test-install passed.