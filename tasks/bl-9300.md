+++
title = "Flaky: spawn_retries_past_transient_etxtbsy fails whenever the box is loaded"
created = 1784957565
updated = 1785124421
claimant = "Ratchet"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
ALREADY FIXED ON MAIN — verified, no code change needed.

The defect was real and is now gone. bl-7a3f ("Close gate flakes under parallel-agent load: spawn_retries_past_transient_etxtbsy races two wall clocks", commit 126de9f, 2026-07-25 18:12) landed the exact fix this ball asked for, ~20h after this ball was filed (2026-07-24 22:32). bl-9300, bl-2061 and bl-7a3f are three filings of one defect.

Shape that landed (the ball's second candidate: budget injected, not wall-clock-shared):
- The test moved from src/prompt/tool/tests/errors.rs to src/prompt/tool/tests/etxtbsy.rs.
- src/prompt/tool/spawn.rs gained SpawnTool::with_etxtbsy_budget(Duration); subprocess.rs carries the budget per-request as SpawnArgs::etxtbsy_budget rather than reading the shared const.
- The shipped ETXTBSY_RETRY_BUDGET is unchanged at 200ms — production behaviour was not widened.
- spawn_retries_past_transient_etxtbsy now injects a 30s budget against the same 40ms fixture hold, so no plausible scheduling stretch can make the hold outlast the budget.
- The sibling spawn_surfaces_etxtbsy_after_budget_exhausted is deterministic in the other direction: its holder keeps the fd open until the executor has already given up, so every attempt is guaranteed to see ETXTBSY against its injected 1s budget.
- The fixture sanity check write_script_helper_is_round_tripped_by_the_fixture wraps its own exec in a 30s envelope for the same reason.

Determinism evidence (Ratchet, 2026-07-26, 16-core box):
- 30 consecutive runs of the 3 prompt::tool::tests::etxtbsy tests, --test-threads=1, under synthetic load (40 spinners; load average 29.6 rising to 40.6): 30/30 pass, 0 failures. The original report saw 5 failures in 6 runs at load 18-27.
- Full gate on the merged state: make check exit 0 — 902 tests pass, 100.00% coverage, 4678/4678 lines.

Disposition: closed with an empty delivery. No source change; the record is the verification. bl-2061 is closed as the duplicate.