+++
title = "the ball pane: a workspace's balls are readable nowhere, and none can be created, claimed, closed or amended from this seat"
created = 1788397351
updated = 1788397351
priority = 2
root_commit = "3efc0d263898c425a0ff2bb042938233e838f436"
+++
yog classes nine ops `control` (docs/PARITY.md §2) that this seat gives no interactable: `balls`, `board`, `workspace-balls`, `create`, `close`, `update`, `assign`, `release`, `marks`.

The window paints workspaces, conversations and a transcript, and nothing about the task store the conversations are working. So an operator seated here can watch work happen and cannot see, file, claim, close or amend the ball it is happening on — the board is the one pane that says what the fleet is FOR.

Until a pane exists these nine are exemption rows in `parity.toml` citing this ball, printed on every run of the parity assertion in `src/snapshot/parity`. Deleting a row when its control lands is the whole of the bookkeeping.