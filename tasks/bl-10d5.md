+++
title = "gate: docs"
created = 1785459718
updated = 1785459856
claimant = "pedantic-lernie"
parent = "bl-e4ef"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
GREEN. Every live mention of the pin now reads 0.0.5; historical mentions (CHANGELOG entries for 0.0.1/0.0.2, USER_STORIES walk records) are left as history.

Updated: README.md x5 (three `cargo install brazen --version =0.0.5 --locked` install commands, the `lernie <version> (brazen 0.0.5)` rendering, the `brazen = "=0.0.5"` manifest line, and the version-mismatch example at L1209 re-based to 0.0.4-found vs 0.0.5-linked); docs/ARCHITECTURE.md §4.4 (`lernie 0.0.1 (brazen 0.0.5)`); src/cmd/mod.rs doc comments x2.

The README is a THIRD reader of the pin's one home and `src/prompt/tests/pin.rs::every_brazen_version_the_readme_spells_is_the_pin` holds every version it spells equal to `brazen_pin()` — it passes, so the prose did not drift.

CHANGELOG.md is not hand-edited: release-plz generates entries from commit subjects and its `commit_parsers` skip gate closes, so this ball's subject becomes the 0.0.3 bullet on its own.

Also recorded the recurrence itself: docs/USER_STORIES.md G6 now notes this is the THIRD stale pin, that the cost this time was a cross-repo double-resolve rather than only the local guard, and that the prose-drift half of the class is shut while the staleness half (nothing notices when brazen publishes) is still open.