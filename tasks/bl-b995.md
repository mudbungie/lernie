+++
title = "found lernie, the seat: the repository, the gate, the wire client and the design"
created = 1788068276
updated = 1788068350
claimant = "OrderSeat"
priority = 5
root_commit = "3efc0d263898c425a0ff2bb042938233e838f436"
+++
The seat severed from yog into its own crate and repo at the version fence (yog REMOTE §12, yog bl-0716).

What this ball lands is the component: the repository and its full gate apparatus (300-line cap, 100% coverage floor, the disclosure scanner ported from yog with its rule table and both fixture directions, the ast-grep confinement rules, the pinned toolchain/deny/tarpaulin), the crate manifest fixed at 0.1.0 with `publish = false`, docs/DESIGN.md as the component authority, and the code that makes a seat a seat at the wire: the mTLS channel, the version preface, the framing, the client-side workspace entries, the envelope the seat carries and routes but does not own, and the one-shot CLI over both.

It does NOT land the window. That is the larger half and it is filed as its own chain — see DESIGN §5. Scoping the first landing to what genuinely runs against a real server, and filing the rest honestly, was the instruction.