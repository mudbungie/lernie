+++
title = "the workspace's configuration is unreachable: no lineage, no config file, no role tuning, and no way to unmake it"
created = 1788397358
updated = 1788397358
priority = 2
root_commit = "3efc0d263898c425a0ff2bb042938233e838f436"
+++
Seven `control`-classed ops with no interactable here: `config`, `lineages`, `model`, `roles`, `effort`, `priority`, `delete-workspace`.

The roster names each wall and its rollups. What the wall is CONFIGURED as — which lineages it holds and what files each one carries, which config commit a read or a write lands on, what its roles are actually set to and how each is tuned for reasoning effort and provider priority — is readable and writable nowhere from this seat. The role tuning arrived on the wire at PROTOCOL 6 and the `roles` read at 7; this end has painted neither.

Sibling absences with their own citations: the provider table and its model lists (bl-b180, bl-e3c5), the registered clients and the tool consent (bl-e53c).

Exemption rows in `parity.toml` cite this ball until the surfaces land.