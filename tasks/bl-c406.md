+++
title = "docs gate: §6 budget collapse bl-f48c"
created = 1783645923
updated = 1783645923
parent = "bl-f48c"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Verify docs/ARCHITECTURE.md §6 no longer describes clamped inheritance / parent-remaining / dispatch snapshot; instead states the derived whole-tree model (depth positional; global ceiling = derive over root_of(branch) vs the single frozen limit, checked by any driver; optional --token-cap local cap noted as future-not-built). budget/mod.rs module doc-comment matches; no stale 'cannot be re-derived' caveat. Terms used are defined (TAXONOMY/ARCH §2.1). No dangling references to remaining/clamp anywhere in docs.