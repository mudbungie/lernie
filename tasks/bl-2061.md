+++
title = "spawn_retries_past_transient_etxtbsy is a load-sensitive gate flake"
created = 1784957520
updated = 1785124449
claimant = "Ratchet"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
DUPLICATE of bl-9300 — both are now closed; the defect is fixed on main. Filed 45 seconds apart by two sessions that each saw the same gate failure.

Primary observation (the flake): fixed. bl-7a3f ("Close gate flakes under parallel-agent load: spawn_retries_past_transient_etxtbsy races two wall clocks", commit 126de9f on main, 2026-07-25 18:12) took this ball's SECOND candidate — make the ETXTBSY window deterministic rather than wall-clock — and explicitly did NOT take the first (the shipped ETXTBSY_RETRY_BUDGET is still 200ms, so production behaviour was not widened and the sibling test was not slowed). The budget is now injected per-executor via SpawnTool::with_etxtbsy_budget (src/prompt/tool/spawn.rs) and carried as SpawnArgs::etxtbsy_budget; the test moved from src/prompt/tool/tests/errors.rs:169 to src/prompt/tool/tests/etxtbsy.rs and injects a 30s budget against the same 40ms fixture hold, so no scheduling stretch can make the hold outlast the budget.

Verified by Ratchet 2026-07-26 on a 16-core box: 30 consecutive runs of the 3 prompt::tool::tests::etxtbsy tests, --test-threads=1, under synthetic load (load average 29.6 rising to 40.6) — 30/30 pass, 0 failures, against the 5-failures-in-6-runs at load 18-27 reported here. Full gate on the same tree: make check exit 0, 902 tests, 100.00% coverage, 4678/4678 lines.

Secondary observation (gate runs dying to an external SIGTERM, "make: *** [Makefile:54: coverage] Terminated" ~13 min in, even under setsid): that source is identified and fixed separately. It was the stop-cascade pgid race — bl-5f0c, "stop cascade pgid discovery can signal the wrong process group — e2e stop tests killed their own gate run under load", commit 24768d0 on main. The e2e stop tests were signalling the wrong process group and killing their own gate run. Not a stray kill from a concurrent agent as guessed here.

Disposition: closed with an empty delivery. Nothing to deliver — bl-7a3f carries the code, bl-5f0c carries the SIGTERM fix, bl-9300 carries the verification record.