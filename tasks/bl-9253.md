+++
title = "Cut 0.0.1: push main, drive release-plz to a tagged crates.io publish"
created = 1784955403
updated = 1785124100
claimant = "Halyard"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
IN FLIGHT — Halyard, 2026-07-26.

DONE
- Stale claim (Gantry, dead session) released; claimed by Halyard.
- Verified pre-push state: local main 29 ahead / 0 behind origin/main, no divergence. Cargo.toml version = 0.0.1. gh secret list empty (correct — f831a89 [bl-6cfb] moved publishing to crates.io trusted publishing via OIDC; no CARGO_REGISTRY_TOKEN needed). ci.yml now resolves the brazen pin from Cargo.toml (make brazen-pin) and pin-caches both bz homes, fixing BLOCKER 1 of the previous attempt (cbdc9d4 [bl-636b], refined by 1588f1c [bl-8366]).
- PR #1 'chore: release v0.0.1' re-checked: OPEN, branch release-plz-2026-07-25T00-17-04Z, MERGEABLE, diff still CHANGELOG.md only (+176/-0). Cargo.toml untouched.
- PUSHED: origin/main e3123ba..39c10a5.

WATCHING
- CI run 30216420512 (push, main) in progress.
- Release-plz run 30216420496 (push, main) in progress — this is the PR-refresh job; the 'release-plz release' job fires separately on workflow_run when CI goes green.

PREVIOUS BLOCKERS, both resolved before this push:
- BLOCKER 1 (bz missing on the runner) — fixed by bl-636b/bl-8366.
- BLOCKER 2 (CARGO_REGISTRY_TOKEN absent) — dissolved by bl-6cfb: trusted publishing (OIDC), configured on crates.io against workflow file release-plz.yml. That file must never be renamed. crates.io already carries lernie 0.0.0 (manual publish), satisfying the trusted-publishing cannot-create-a-crate rule.

NOT CLOSING until crates.io shows lernie 0.0.1.