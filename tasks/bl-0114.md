+++
title = "gate: tests"
created = 1785124190
updated = 1785125412
claimant = "Gimbal"
parent = "bl-8fb1"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"

[[blockers]]
id = "bl-8fb1"
on = "claim"
+++
PASS. `make check` exit=0 on the delivered state: `fmt-check`, `clippy -D warnings`, `cargo tarpaulin --fail-under 100` reporting **100.00% coverage, 4678/4678 lines**, and `test-install` (`cargo test --test install`) ok. The close gate re-ran the same `make check` via the pre-commit hook against main folded in and reported 100.00%, 4839/4839 lines, with `make_install_lays_down_skeleton_idempotently ... ok`.

Targeted proof of the change itself, since the install test is `cfg_attr(tarpaulin, ignore)` and only `test-install` exercises it: shadowed `bz` on `PATH` with a shim reporting `0.0.1` so `install-bz`'s pin guard mismatches and the real `cargo install brazen --version "=0.0.4" --locked` runs. `cargo test --test install` passed in 145s; brazen 0.0.4 landed in `target/install-test-cargo-root/bin/bz` (`.crates.toml`: `brazen 0.0.4`), and `~/.cargo/bin/bz` kept its exact sha256 `b6506c4d9ff6...` and mtime `2026-07-25 01:53:24` across the run.

Two earlier gate attempts died on load-induced infrastructure flakes at fleet load ~90 (tarpaulin `ECHILD: No child processes` during ptrace, then a bare SIGTERM / `Error 143`); neither was an assertion failure, and the clean run stands.