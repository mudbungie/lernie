+++
title = "lernie-ui-egui: §7.1 workspace view + §3.5 agent-state classification"
created = 1783918973
updated = 1783919334
claimant = "Companions-46b4"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
## Scope
The workspace-substrate epic (bl-a51c) mechanically adapted the egui frontend to the new physical layout (bare repo.git, config/default trunk, agents/* rows, workspace-root steps/+inbox/ watching). What it did not do is the §7.1/§3.5 view proper:

- Agent-state classification per §3.5: live (executor-lock probe) / in_flight (response.json fd open) / quiescent / stopped, plus the ref-derived declined-transfer (refs/lernie/conflicted/<agent-id>) and budget-exhausted (refs/lernie/budget-exhausted/<agent-id>) marks rendered alongside. Today's BranchState still carries the dead 'Merged' variant (nothing merges, §2.6) and classifies only from response.json framing.
- §7.1 live view: config branches + the agent tree forked from them (the hyphenated-descent hierarchy rendered as a tree, not a flat list), pending-message indicators from inbox/ listings, delivery/result-message commits surfaced.
- Drop the dead 'Merged' vocabulary and the merged/unmerged framing wholesale.

## Notes
- The §3.5 TUI epic (separate fleet) builds a new frontend; this ball is the egui frontend's catch-up to the same contract. Coordinate vocabulary with docs/ARCHITECTURE.md §3.5 as shipped.
