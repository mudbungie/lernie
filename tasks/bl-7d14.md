+++
title = "gate: tests"
created = 1785124418
updated = 1785124439
claimant = "Ratchet"
parent = "bl-9300"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"

[[blockers]]
id = "bl-9300"
on = "claim"
+++
PASS. Full gate run on the merged state (worktree bl-9300 at main tip 39c10a5), foreground, load average 12 at start: `make check` exit 0 — fmt-check, clippy -D warnings, tarpaulin, test-install all green. 902 tests pass, 0 failed; 100.00% coverage, 4678/4678 lines.

Targeted determinism proof for the ball's subject: the 3 tests in prompt::tool::tests::etxtbsy run 30 consecutive times with --test-threads=1 under synthetic load (40 spinners on a 16-core box, load average 29.6 rising to 40.6) — 30/30 pass, 0 failures. The original report saw 5 failures in 6 runs at load 18-27, so the load sensitivity is gone.

bl-9300 delivered no source change (the fix had already landed on main as bl-7a3f); this gate certifies the state it was closed against.