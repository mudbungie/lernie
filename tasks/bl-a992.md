+++
title = "gate: tests"
created = 1785124069
updated = 1785124448
claimant = "Capstan"
parent = "bl-d32a"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"

[[blockers]]
id = "bl-d32a"
on = "claim"
+++
PASS. `make check` ran green twice on the bl-d32a end state — once standalone and once as the pre-commit gate inside `bl close`: `100.00% coverage, 4678/4678 lines covered, +0.00% change in coverage`, every test binary ok, fmt-check and clippy -D warnings clean, and the uninstrumented install contract `1 passed`.

New tests: `tests/hooks.rs`, 6 tests, all passing (a commit on main pushes; a commit-tree+update-ref delivery pushes; a --no-ff merge pushes; a side-branch commit pushes nothing; an unreachable origin warns without failing the commit; no origin is silent). They are coverage-neutral by construction — tarpaulin never counts `tests/` in the denominator — and they never touch the real remote: every fixture builds its own local bare repository as `origin`.

Mutation-checked rather than assumed: replacing the hook body with `exit 0` fails 5 of the 6.

300-line cap: tests/hooks.rs is 255; `.githooks/` is cap-exempt by the pre-commit hook's own exempt pattern.