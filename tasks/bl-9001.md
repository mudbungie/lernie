+++
title = "a tool-injection seam on Fx: extra tool definitions at prompt assembly plus an execution router consulted ahead of SpawnTool"
created = 1786757186
updated = 1786757186
priority = 2
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Upstream ask from yog's client/server split (yog docs/REMOTE.md §5, adopted yog bl-b9a2; operator accepted dynamic tools 2026-08-13, reading DESIGN_MCP_BRIDGE.md's pin doctrine as standardizing the integration pipe rather than refusing dynamism). yog needs to (a) place extra tool definitions in front of a model at prompt-assembly time — a client-management tool plus tools a remote client advertises — and (b) route designated tool invocations to its own engine instead of the local spawn path, so a conversation can teleoperate another machine's advertised tools.

Deliverable, one narrow shape-agnostic seam — the driver embedder's injection idiom (Fx) rather than a new public module:

1. A design doc (docs/DESIGN_*, defining its own terms per the terminology rule) recording the seam and its containment story against DESIGN_MCP_BRIDGE.md: injected tools are individually named (never a multiplexer — that ruling stands), the injected set changes only by the embedder's explicit act (append-only prompt/cache discipline holds; the embedder pays any prefix rebuild knowingly), and adjudication (tool_control) is untouched — injected invocations are adjudicated exactly as local ones, before routing; the control stays a predicate.
2. Fx (or the equivalent embedder surface) gains an optional hook with two capabilities: extra tool definitions + their names for the grant union (the compactor's injection is the precedent — both halves, declaration and permission, or a tool is declared then refused), and a router consulted in the execute path that answers Some(outcome-or-error) for names it owns and None to fall through to SpawnTool. Router obligations stated where the executor contract lives: it owns the per-invocation input/output disk record like any executor, carries a deadline, and a vanished remote endpoint is an in-band error result, never a hang.
3. Wiring stays private: prompt-assembly splices the extra definitions (both drive entry points), the grant gate unions the injected names, the execute path consults the router ahead of the spawn resolution. No new verb, no new config key.
4. Gates: tests (100%, all pass), docs (ARCHITECTURE tool sections updated), alignment (ARCHITECTURE/PRINCIPLES/TAXONOMY coherent; new terms defined in the introducing doc).

Consumer note: yog consumes lernie via exact crates.io pins, so this lands, releases, and yog bumps its pin — keep the surface minimal since it is a published-crate commitment.