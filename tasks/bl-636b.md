+++
title = "ci.yml never installs bz — five real-bz e2e tests fail on GitHub CI"
created = 1784955549
updated = 1784955555
claimant = "Marrow"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Release blocker (bl-9253). First-ever CI run on origin (30135891554) failed: 5 e2e tests (e2e::advance_cli, prompt_end_to_end, prompt_retry, stop_cli x2) exec the REAL bz binary ('adapter subprocess: No such file or directory'). They pass locally only because make install put ~/.cargo/bin/bz on PATH. .github/workflows/ci.yml installs only Rust + tarpaulin. Fix: add a brazen install step before make ci, deriving the pin from Cargo.toml's brazen '=X.Y.Z' line the way Makefile:33 does (single source of truth — do NOT hardcode the version in ci.yml). Consider caching (~/.cargo/bin + registry) so the install doesn't cost minutes per run. Verify by pushing the worktree? No — the merge to main will be pushed by release ops; validate locally what you can (act if available, else lint the workflow YAML and dry-check the derivation command).