+++
title = "Author docs/USER_STORIES.md: the promise suite lernie 0.0.1 is evaluated against"
created = 1784955403
updated = 1784959198
claimant = "Emberly"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Design ball; deliverable is docs/USER_STORIES.md, a living tracked doc.

== STATE (2026-07-25) ==
DOC IS COMPLETE. 415 lines, 24 stories (US-01..US-24), committed in the
bl-0135 worktree at /home/u/.local/state/balls/plugins/bl-delivery/home/u/dev/lernie/bl-0135
on branch work/bl-0135. README.md line 8 links it. NOT yet closed.

WARNING: the commit that carries the doc landed with the WRONG message
(another agent in the same session clobbered the shared scratchpad file
/tmp/.../scratchpad/msg.txt, so the commit reads "[bl-5156] Settle a
stopped model call..."). Content is correct (README.md +1, docs/USER_STORIES.md
+415). Fix with: git commit --amend -F <scratchpad>/bl0135-emberly-msg.txt
(that file holds the correct bl-0135 message). ALWAYS use a uniquely
named message file - the scratchpad is shared across this session agents.

== REMAINING STEPS TO CLOSE ==
1. cd <worktree>; git merge main --no-edit   (stash README if it conflicts)
2. Refresh stale story statuses for anything that landed since 8d8638c.
   Already folded in: bl-4a6c (parent revival SHIPPED - was G1),
   bl-1c2e (coverage flake), bl-2503 (tool cwd). STILL TO CHECK: bl-ae66
   (skill descriptions now compose -> ARCH 3.3 "Shipped-state gap" note
   and US-12 may be stale), bl-bdc7, bl-a1a1, bl-5156.
3. git commit --amend -F <scratchpad>/bl0135-emberly-msg.txt
   with PATH=<scratchpad>/bzroot/bin:$PATH  <-- REQUIRED, see below.
4. DELIVERY IS SERIALIZED. Hold bl close until BOTH [bl-bdc7] and
   [bl-a1a1] appear in `git log --oneline main`. Then:
   cd /home/u/dev/lernie && bl close bl-0135 --as Emberly -m "..."
   After landing, Osprey (bl-aabb) goes next - the landing is his signal.
5. Close gates bl-1d8c (tests), bl-bd8e (docs), bl-d5c4 (alignment).

== GATE RECIPE (non-obvious, cost me hours) ==
* The e2e tests exec the real bz. ~/.cargo/bin/bz keeps flipping between
  0.0.3 and 0.0.4 as sibling agents install; with 0.0.4 the version guard
  fails 5 e2e tests. A private pinned copy lives at
  <scratchpad>/bzroot/bin/bz (0.0.3). Prepend it to PATH for any commit:
  PATH=<scratchpad>/bzroot/bin:$PATH git commit ...
  Rebuild if lost: cargo install brazen --version =0.0.3 --locked --root <scratchpad>/bzroot
* Do NOT wait on the gate with `read -t N -u 3 3< <(:)` - that reads a
  closed fd and returns instantly, so the wait is a no-op. Use
  `for i in $(seq 1 54); do [ -f done ] && break; timeout 10 tail -f /dev/null; done`.
* Never let a Bash call hit its 600s timeout while the gate runs - the
  harness SIGTERMs the process tree and kills the gate. Keep waits <=540s.
* Last full gate: 883 tests, 0 failures, exit 0.

== VERIFICATION STATUS ==
Verified hands-on (scratch LERNIE_HOME + live wire via local ollama using
the models.yaml adapter: override): US-02 prime, US-03/04 new, US-06
prompt, US-08 read_file, US-09 bash, US-13 message+revival, US-15 scan,
US-19 bundle, US-20 replay, US-24 adapter guard, and the child-runs half
of US-17. Test-backed: US-05, US-07, US-10, US-14, US-16, US-18, US-21,
US-22, US-23. Marked unverified (a later eval pass must fill): US-01
make install, in_flight fd-close signal, work-product transfer with a
real work product, compaction merge via a real checkpoint, lernie tool
message/load_skill as subprocesses, any suite task vs a live model.
All 35 cited test names were confirmed to exist.

== GAP LIST (doc section 11) ==
G1 RESOLVED by bl-4a6c (child result deposit did not revive a quiescent
   parent; now dispatch::terminal::revive_parent + parent_revival.rs).
G2 README documents a terminal compactor that ARCH 2.7 deleted; its
   inspection commands (show <branch>:summary/001.md) fail. OPEN.
G3 README + doc comments in inbox/scan/mod.rs and dispatch/result_deposit.rs
   claim children do not run; they do (worker.rs is gone). Consequence:
   NO integration test proves children run. OPEN.
G4 lernie dispatch compactor does not run the compaction merge README
   attributes to it (0 merge refs in dispatch_cli.rs). OPEN.
G5 lernie message accepts a nonexistent recipient (exit 0, creates
   inbox/<ghost>/), vs ARCH 2.11 "addressed to an existing agent". OPEN.
G6 brazen pin staleness breaks the repo own gate, not just users - see
   gate recipe above. Possibly covered by bl-8c92 (pin bump). CHECK.
G7 agent-eval production wiring dangles: default --agent lernie-eval-agent
   names a nonexistent binary, and LERNIE_EXPERIMENT is read by nothing
   under src/, so experiments never reach the harness. OPEN.
G8 shipped template/workflow.yaml declares spawn_root_agent, which
   workflow_actions.rs has no interpreter arm for (not reachable today).
G9 --stop-children has no integration proof (only the no-holder
   short-circuit). Possibly touched by bl-5156. CHECK.
G10 anthropic shipped default (claude-sonnet-5) still unvalidated on the
   live wire; needs a user credential.