+++
title = "auto-merge release PRs: the release is already CI-gated post-merge, so merge the bot PR on arrival"
created = 1788398930
updated = 1788399267
claimant = "Mergesmith"
priority = 2
root_commit = "3efc0d263898c425a0ff2bb042938233e838f436"
+++
OPERATOR RULING (2026-09-03): release PRs auto-merge on green CI. The mechanism and its two shaping facts are stated in yog's twin ball (filed the same night — search yog's store for auto-merge release PRs): bot PRs trigger no pull_request workflows so the PR carries no checks; and this repo's Release-plz workflow already gates the release job on the CI job post-merge (observed live: run 33611717922, red CI skipped the publish). So the mechanism merges the bot's release-plz-* PR on arrival — a workflow job triggered on Release-plz completion, merging open PRs whose head matches release-plz-* AND whose author is the Actions bot, via gh with the repo token (confirm the token may merge; a refusal is a named operator act, recorded). Never merge a human's PR. No session URLs in anything the workflow writes. Coordinate with bl-09c8 (the CI vulkan fix touching the same workflow file) — land after it.