+++
title = "prove the MCP bridge seam: fleet Slack swap in deployment space (per bl-3c76 design; zero lernie product code)"
created = 1785724751
updated = 1785724751
priority = 3
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
tags = ["integration"]
+++
Per docs/DESIGN_MCP_BRIDGE.md (bl-3c76, §7): swap the fleet demo's mock lernie-tool-slack_read / lernie-tool-slack_post for tools pinned from a real Slack MCP server via a deployment-owned bridge binary in the lernie-tool-<name> slot. Stdio transport only; bridge config owns the server command, allowlist, and credential; one server per tool invocation. Pass criterion: real round-trip through the ordinary result envelope with an EMPTY `git -C ~/dev/lernie diff` — everything lands in deployment space (~/ops/fleet or a bridge repo of its own). Needs: a chosen Slack MCP server and a workspace credential. Refusals inherited from the design: no core MCP, no multiplexer tool, no resident server, no second transport.