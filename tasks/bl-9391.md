+++
title = "seed models.yaml declares gpt-5.x provider 'codex' — a provider name brazen's table doesn't have"
created = 1785645030
updated = 1785645030
priority = 2
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Found via yog bl-662f investigation 2026-08-01: lernie's prime seed writes models.yaml with gpt-5.4: provider: codex (and template/providers.yaml likewise), but brazen's provider row for that family is named openai-chatgpt (~/.config/brazen/config.toml has no codex row). Any consumer that picks gpt-5.4 as default then fails 'unknown provider `codex`' on the next prompt (bz --list-models --provider codex → exit 78; --provider openai-chatgpt → succeeds, same models). Fix the seed data so the shipped provider names match brazen's actual table. Evidence: ~/.local/share/yog/world/lernie/models.yaml, yog src/world/seed.rs (seed content comes from lernie prime, not yog).