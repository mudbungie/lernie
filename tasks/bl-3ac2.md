+++
title = "commit-identity guard test + changelog normalization"
created = 1785125363
updated = 1785132936
claimant = "waxier-lfin"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
DELIVERED (waxier-lrw, 2026-07-27). Follow-on to the 2026-07-26 main history rewrite (every author+committer on main is `mudbungie <mudbungie@gmail.com>`; all Co-Authored-By trailers stripped; old tip 2c942f9 -> cd8aa45, tree-identical).

1. tests/commit_hygiene.rs — the guard, as specified. Armed ONLY by the marker file `$XDG_CONFIG_HOME/lernie/enforce-commit-identity` (default `~/.config/lernie/enforce-commit-identity`), which lives OUTSIDE the repo: absent (public CI, every other clone) the test returns without asserting; present it walks all of `refs/heads/main` via `std::process::Command` git (`log -z --format=%H%x1f%an…%x1f%B`, git env scrubbed like tests/hooks.rs) and asserts per commit: author and committer are `mudbungie <mudbungie@gmail.com>` or `github-actions[bot]` (bot allowed BY DESIGN — release-plz authors the release commit as it; documented in the code), no `Co-Authored-By` (case-insensitive), and no `t@t.local` / `orionriver` anywhere in identity or message. Unreadable git/ref => skip (passing). No new dependencies. `tests/` is outside tarpaulin's denominator, so the guard is coverage-neutral. VERIFIED both ways: armed run asserts over 212 commits and passes; a run with XDG_CONFIG_HOME pointed at a tempdir prints `commit-identity guard disarmed: …` and passes.

2. CHANGELOG.md — normalized. main's post-rewrite CHANGELOG (the merged release PR #1's generated 205 lines) was NOT uniform: it carried duplicate bullets (bl-cd6b twice, bl-a51c twice, bl-8094 twice, bl-d135/bl-70b9/bl-fdb9 with doubled refs), bare gate-close bullets (`alignment [bl-4af9] [bl-09d4]`, `docs [bl-a51c] [bl-f48f]`, `docs gate: …`, `tests gate green: …`, `reflect current state`), and multi-paragraph commit bodies pasted verbatim as single bullets (bl-9e9b, bl-f739, bl-507a, bl-4a9b, bl-660b …). Now 187 lines: one imperative bullet per shipped change, every `[bl-xxxx]` trail preserved, no short shas, no identity leakage (grep for orionriver / t@t.local / co-authored is clean), keepachangelog header and the `## [0.0.1]` compare link untouched. A short paragraph under the header states the rule so the next hand-edit follows it.

3. release-plz.toml `[changelog].commit_parsers` — a first parser `{ message = '(?i)^(gate:|(v[0-9][0-9.]* )?(docs|tests|alignment)( gate)?( green)? *(:|\[bl-))', skip = true }` so FUTURE generated changelogs come out uniform without hand-editing. The pattern requires a `:` or `[bl-` right after the gate label, so real subjects that merely open with the word (`tests/install.rs mutates …`) are not swallowed; a gate close written in free prose still needs hand-pruning. Known and accepted consequence: a release window containing only gate commits generates no entries.

4. Two hygiene fixes found while closing bl-53e3, in scope as commit hygiene:
   - `check.exit` was committed onto main by that close (6cf98c1) — `bl close` stages EVERY untracked file in the worktree, so an agent's `make check` scratch rides onto main. Removed here.
   - .gitignore now ignores `*.profraw` (tarpaulin's root `build_rs_cov.profraw`, which also aborted a close on the 300-line cap: "build_rs_cov.profraw has 5538 lines"), `check.log`, `check.exit`. Ignoring is the fix, not cleaning: the close stages what is not ignored.
   - README gained a `### Commit-identity guard (opt-in, per machine)` subsection under Contributing documenting the marker and the bot carve-out.