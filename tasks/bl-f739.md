+++
title = "Design: brazen as the provider layer — ARCHITECTURE/TAXONOMY/PRINCIPLES fold (Draft v0.4)"
created = 1783464230
updated = 1783464230
parent = "bl-00ae"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Deliverable: living-document edits, no code. docs/ARCHITECTURE.md: bump to Draft v0.4; rewrite §4 around brazen's pipe contract (brazen specs authoritative; invocation bz --json --provider <row>, CanonicalRequest on stdin; vocabulary linked from the pinned brazen crate — types only, data plane stays subprocess per §3.4); terminal event message_stop -> {"type":"end"}; response.json = appended attempt segments, each ending in exactly one end line, last segment authoritative; harness-owned retry (retryable() computed from the linked CanonicalError, cap/backoff in workflow.yaml); SIGTERM = immediate adapter death, missing trailing end IS the stop signature (replaces the 5s adapter flush contract; tools keep SIGTERM+5s); auth/endpoints delegated to brazen config+credstore (auth_env/endpoint_env retired); global providers.yaml -> models.yaml (model capabilities only), per-repo providers.yaml roles name brazen rows; §2.1 terms: brazen, attempt, canonical event/request; §2.9/§3.5 classification rewrites; §8 api_calls = derived segment count; §12: insert v0.6 (this epic), renumber v0.7-v0.9, budgets into the workflow milestone, new v1.1 sandboxed-tools milestone sketch; §11 sandboxing entry points at v1.1. docs/TAXONOMY.md §2 provider-adapter entry updated (the binary is bz). docs/PRINCIPLES.md 'Integrations are external binaries' updated. Also record disposition of docs/LHCS.md + docs/WDF.md (untracked May-era drafts, superseded).