+++
title = "check.exit is tracked on main and ships in the 0.0.1 crate tarball"
created = 1785129699
updated = 1785129760
claimant = "capshead"
priority = 2
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Release-relevant, found 2026-07-26 while landing bl-2e28: 'check.exit' — a one-byte make-check exit-code litter file containing '0' — was committed to main by bl-53e3 (6cf98c1). Cargo.toml's exclude list does not cover it, so it ships in the crates.io tarball when 0.0.1 publishes. Sweep before the tag: git rm it, and either add it to .gitignore or fix whatever make target writes it into the tracked tree. Related hygiene in the same sweep if wanted: .gitignore has no *.profraw entry, and tarpaulin's llvm engine litters worktrees with them. Filed by the balls-repo orchestrator lane; whoever holds bl-9253 (the release drive) probably wants this closed before the release-plz run that mints the tag.