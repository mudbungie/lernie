+++
title = "the conversation's deeper records reach no gesture: its own row, one step's drill-in, and the undelivered mail"
created = 1788399124
updated = 1788399124
priority = 2
root_commit = "3efc0d263898c425a0ff2bb042938233e838f436"
+++
Three of bl-2cf7's exemption rows repoint here — the seam is DEPTH: the facts under the steps list bl-2cf7 painted, and the row's own standing.

- `agent` answers the conversation's own row whole: state, flight, presence, context ceiling, spend (tokens + attribution), seats, marks, the held tool call, strip, tip, failure. It is the largest reply shape on the exemption ledger (21 top-level fields, five nested objects) and was scoped out of bl-2cf7 for exactly that — a header over the records pane is the likely home.
- `step` answers one step's four record files, its response events, every tool call's input/output and the capture logs — the drill-in under the steps list, addressed by the `seq` bl-2cf7's rows now paint.
- `inbox` answers the undelivered mail: deposits parsed where parseable, raw beside them.

The pattern is bl-2cf7's own: typed decoder in src/reply, fixture moved corpus/unreadable/ -> corpus/answers/, painted inside the records pane bl-2cf7 founded, control tagged act:<op>, exemption line deleted. `step` wants a control per steps row (the seq is the address); `agent` and `inbox` are pane-standing reads on the same conversation.