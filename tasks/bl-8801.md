+++
title = "Crates.io packaging: metadata, exclude repo-development files, resolve the eval island"
created = 1784955529
updated = 1784956237
claimant = "Junction"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
Harrow audit findings 3+4+5. DELIVERED (Junction, 2026-07-24).

(a) METADATA. Cargo.toml now carries:
  keywords = ["agent", "llm", "git", "harness", "cli"]      (5, the crates.io max)
  categories = ["development-tools", "command-line-utilities"]  (official slugs)
  rust-version = "1.88"
MSRV correction: the ball proposed 1.85 (the edition-2024 floor); the crate actually needs 1.88. Verified by building the workspace under both installed toolchains — RUSTUP_TOOLCHAIN=1.85 cargo check --workspace fails with 4x E0658 "`let` expressions in this position are unstable" (let chains, e.g. src/prompt/mod.rs parse_brazen_pin, crates/agent-eval/src/runner.rs) and 1.88 builds clean. The resolved normal+build dependency graph floors at 1.85 (brazen/clap/ureq/indexmap/hashbrown), so the crate source, not a dependency, sets the floor. (The 1.86 icu_* packages are dev-dependency-only, via httpmock->url->idna, and do not bind consumers.)

(b) EXCLUDE. exclude = ["/.balls", "/.githooks", "/.github", "/AGENTS.md", "/CLAUDE.md", "/Makefile", "/docs", "/experiments", "/release-plz.toml", "/rust-toolchain.toml", "/scripts", "/tarpaulin.toml", "/tests"].
cargo package --list: 275 files -> 242 (-33). After-list by top level:
  213 src/, 10 schemas/, 6 template/, 5 skills/, 1 install/models.yaml,
  README.md, LICENSE, Cargo.toml, Cargo.toml.orig, Cargo.lock, .gitignore, .cargo_vcs_info.json
Nothing else. The include_dir!/include_str! trio (template/, schemas/tools/, skills/) plus install/models.yaml are all still in; schemas/ also stays because src/config/tests/schemas_golden.rs and src/e2e/stop_cli.rs read it via CARGO_MANIFEST_DIR. tests/ went out whole (integration gates + the tests/suite/ evaluation data); cargo does not build test targets during package verification, and none are auto-discovered when the directory is absent. crates/ was never in the tarball (separate workspace member).
VERIFIED: `cargo package -p lernie` (which compiles the extracted tarball) exits 0. The generated manifest keeps rust-version = "1.88" and normalizes the pin to [dependencies.brazen] / version = "=0.0.3" — the spelling src/prompt/mod.rs parse_brazen_pin already handles, so the §4.4 load-time version guard still resolves in the published crate.

(c) EVAL ISLAND. No driver written (orchestrator decision). Instead, made honest:
  - crates/agent-eval/src/main.rs: the phantom `--agent lernie-eval-agent` default is DELETED; --agent is now a required clap argument. A missing driver is now a usage error before any work starts, instead of one failed spawn per task.
  - crates/agent-eval/src/agent.rs: CommandAgent::dispatch wraps a spawn failure so it names the program — `agent-eval: --agent lernie-eval-agent: No such file or directory (os error 2)`. Covered by tests/agent.rs.
  - Makefile `eval`: AGENT is required, with an actionable usage line naming the missing driver; the $(if AGENT,...) conditional is gone.
  - README: `--agent` in the example; a new "You must supply the driver" block documenting the FULL driver contract, LERNIE_EVAL_REPORT included (two lines — workspace path, agent id — optional, only affects bundleability, never pass/fail); `make eval` added to the build-targets table; the rust-version/rust-toolchain split noted at the top; a new "What gets published" subsection describing the exclude boundary.
  - Contradiction resolved: tests/suite.rs and tests/suite/README.md now say the same thing — the runner crate shipped and reads this data, but the driver it invokes does not exist, so the suite has never been measured against a live model.
  - docs/ARCHITECTURE.md §9.3 gains a "Not yet runnable end to end (bl-8801)" note; §11 v0.10 "Shipped shape" now states the success criterion is NOT met.
  - Deferred driver ball: bl-2e28 (gates bl-00fe tests / bl-23da docs / bl-f54b alignment).

GATE: make check green (fmt-check, clippy -D warnings, tarpaulin 100% workspace coverage).