+++
title = "Adopt brazen canonical vocabulary: link crate for types, dual-vocabulary readers"
created = 1783464230
updated = 1783464230
parent = "bl-00ae"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"

[[blockers]]
id = "bl-f739"
on = "claim"
+++
Per ARCHITECTURE.md v0.4 §4.4 (land D1 first; read it). Link the brazen crate at an exact pin (=0.0.x) for the canonical serde types (CanonicalRequest/Message/Content/Tool, Event/ContentKind/Delta/Usage/FinishReason, CanonicalError + retryable()). Add a segment/classification module implementing the v0.4 rules: a segment is the lines through a {"type":"end"} line; last segment authoritative; file closed WITHOUT trailing end = stopped (kill signature); last segment carrying Error = failed attempt. Teach every response.json reader — await tool stopped/error signatures (src/prompt or wherever v0.4-P3 landed, see bl-c9ec), lernie-ui-egui streaming tail + git_tree branch-state classification — to accept BOTH the legacy v0.3 vocabulary (message_stop terminal) AND brazen v=1 (end terminal), so the writer swap in the follow-on ball merges green. Add brazen-vocabulary fixtures alongside legacy ones. 100% coverage holds.