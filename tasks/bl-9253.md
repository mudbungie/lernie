+++
title = "Cut 0.0.1: push main, drive release-plz to a tagged crates.io publish"
created = 1784955403
updated = 1785124090
claimant = "Halyard"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
+++
PUSHED + CI RED. State as of 2026-07-25.

DONE
- origin/main: pushed 92ccd7a..e3123ba (82 commits; first push since 2026-05-02). origin/main == local main.
- Release-plz workflow ran for the first time. run 30135891552: 'release-plz PR' SUCCESS, 'prune superseded release branches' SUCCESS, 'release-plz release' + binaries SKIPPED (correctly — the build gate held).
- PR #1 'chore: release v0.0.1' is OPEN on branch release-plz-2026-07-25T00-17-04Z. It proposes version 0.0.1 (NO bump to 0.0.2 — release-plz saw 0.0.1 was never published and staged it as-is). Diff is CHANGELOG.md only: +176/-0, new file, one 0.0.1 section listing the whole history. Cargo.toml untouched. NOT MERGED (per instruction).

BLOCKER 1 — CI RED on main (run 30135891554, 'make ci' exit 2, 829 passed / 5 failed):
  e2e::advance_cli::message_launches_a_detached_advance_chain_that_batons_through_tools
  e2e::prompt_end_to_end::prompt_subcommand_persists_conversation_without_terminal_compaction
  e2e::prompt_retry::retryable_529_then_clean_writes_two_segments_and_completes
  e2e::stop_cli::stop_cascades_sigterm_and_leaves_response_without_terminal_end
  e2e::stop_cli::stop_lands_during_tool_execution_via_inbox_lock_fd
All five: 'lernie prompt: adapter subprocess: No such file or directory (os error 2)'.
ROOT CAUSE: these e2e tests exec the REAL brazen 'bz' binary (src/e2e/prompt_end_to_end.rs:3 — 'The model call execs real bz (§4.4); BRAZEN_CONFIG points bz at a fixture provider row whose endpoint' is an httpmock). .github/workflows/ci.yml never installs brazen, so 'bz' is not on the runner's PATH. Locally it passes only because 'make install' put ~/.cargo/bin/bz there (Makefile:130, cargo install brazen --version =$(BRAZEN_PIN) --locked). The real-bz e2e tests landed in 70ac772 [bl-231c], inside this unpushed window — CI had never seen them.
FIX (needs a worktree ball; NOT mine to make — I am ops-only): add a brazen install step to ci.yml before 'make ci', deriving the version from Cargo.toml the same way Makefile:33 does.

BLOCKER 2 — CARGO_REGISTRY_TOKEN absent. 'gh secret list' returns empty. The release job cannot cargo publish without it. User-only escalation.

NOTE: CI on PR #1 (run 30135925798) is conclusion 'action_required' — GitHub is holding the bot-authored PR's workflow for manual approval. Cosmetic for the release path (the release job gates on the push-to-main CI run, not the PR's), but PR #1 shows no green check until someone approves it.

REMAINING PATH TO A PUBLISHED 0.0.1
1. Fix ci.yml so 'bz' is installed; land it on main; push.
2. CI goes green on main -> Release-plz fires via workflow_run -> 'release-plz release' runs cargo publish for 0.0.1, then tags v0.0.1, cuts the GitHub Release, and release-binaries attaches the linux-gnu archive.
3. Step 2 needs CARGO_REGISTRY_TOKEN present BEFORE that green CI run, else the publish step fails.
IMPORTANT: merging PR #1 is NOT what triggers the release. Any green CI on main releases whatever unpublished version main carries. PR #1 only adds CHANGELOG.md — merge it before the green run if the published 0.0.1 should ship with a changelog.

NOT CLOSING: release is not cut until crates.io shows lernie 0.0.1.