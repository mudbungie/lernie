+++
title = "anthropic provider setup: credential + validate the shipped default over the live wire"
created = 1784698625
updated = 1784698625
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"
tags = ["smoke"]
+++
## Why
The shipped default (worker -> anthropic / claude-sonnet-5) has never been validated on a live wire: this machine's bz has only 'codex' and 'local' provider rows ('bz --list-models --provider anthropic' -> exit 77). bl-06d5's smoke pipeline is proven green via codex; the default path is the remaining unknown — and the fake-id bug class (bl-3157, claude-sonnet-4-7) is exactly what only a default-path live run catches.

## Deliverable (needs the user — credential material)
1. Configure the anthropic provider for bz: 'bz --login --provider anthropic', or export ANTHROPIC_API_KEY / BRAZEN_API_KEY.
2. Run 'make smoke' with NO overrides (pure shipped default) and record the output in this ball.
3. Green run => close. A wire/default bug => file it and close this as the discovery.

No code expected; if the run exposes a wrong embedded default, that fix is its own ball.