+++
title = "gate: tests"
created = 1785124820
updated = 1785129530
claimant = "Parrel"
parent = "bl-f021"
root_commit = "12899370c9ec7a5ed7f8e26d3d4fb914ea6c3310"

[[blockers]]
id = "bl-f021"
on = "claim"
+++
PASS. \`make check\` on the merged tree (work/bl-f021 with post-bl-1c94 main folded in), first run, no flake re-runs needed: **936 passed, 0 failed, 100.00% coverage (4918/4918 lines)**. Re-run again by the close gate itself (bl close pre-commit hook) — same result, +0.00% coverage change.

New tests this ball: \`src/e2e/compaction_wire.rs\` (the QA symptom end to end over real \`bz\` against a recording provider row — reverted against the fix it fails with the ball's literal symptom, a \`tools\` array of only \`["write_summary","mark_for_deletion"]\`), \`src/prompt/tests/compaction_toolset.rs\` (3 hop-level tests: inherited-schema declaration, the in-band decline with a zero-invocation executor assertion, and the no-tools worker hop unchanged), plus election/injection/closure units in \`src/prompt/dispatch/tools/tests.rs\` and \`refusal\` units in \`src/prompt/compactor/mod.rs\`.